// Generated by scripts/contract-gen.mjs - do not edit.
// Regenerate: pnpm contract:gen [--url <openapi.json url>]
/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */


export interface paths {
  "/api/scan": {
    /**
     * Scan a domain, MCP server URL, or MCP App URL for agent-readiness
     * @description Runs a full agent-readiness scan on the given URL. Accepts a domain, MCP server URL, or MCP App URL (server that supports the MCP Apps extension `io.modelcontextprotocol/ui`) - the server auto-detects which kind of input was provided and selects the appropriate check set. Catalog-style listing pages are folded into the `mcp` kind by classifying the first validated embedded MCP URL. Returns score, grade, and detailed layer breakdown. The response includes an optional `urlKind` field indicating the detected kind ('domain', 'mcp', or 'mcp-app'). Scoring completes within the request, but deeper analysis can continue asynchronously afterwards: when the returned analysisStatus is 'partial', the response is a 202 Accepted with a Location header pointing at the polling endpoint for the remaining work; a 200 means analysis is already complete. For real-time progress updates, use GET /api/scan/stream which serves a text/event-stream. A complete stored result younger than the freshness window is returned as-is (servedFromCache, resultAgeSeconds, Age header) without running or persisting a scan - pass force: true, or widen/narrow maxAgeSeconds, to control that. Rate limited two ways: 10 requests per minute per IP (burst) and a durable daily scan budget shared with the other scan entry points - both return 429 with a Retry-After header.
     */
    post: operations["scanDomain"];
  };
  "/api/v2/scan": {
    /**
     * Instant-trigger entry point for the v2 async scan lifecycle (rollout, gated)
     * @description Phase 1b of the scan lifecycle redesign (see docs/plans/scan-lifecycle-prd.md). Validates the input (rate-limit 10/min/IP, Zod, isValidUrl, reachability, URL-kind classification), de-dupes against any in-progress v2 row for the domain, INSERTs the scans row at status="pending" with is_current=true and flow_version='v2', and returns the scanId plus a pollUrl. The downstream pipeline (Stage 1 context + static checks, Stage 2 deep checks via the Fly worker, Stage 3 finalize) is NOT YET WIRED in this PR - rows created here stay at status="pending" until follow-up PRs ship Stage 1 and the recovery cron. The endpoint is gated behind the SCAN_V2_ENABLED env flag and returns 503 in environments where it is unset, so external callers must not rely on it before cutover. The v1 read paths (/api/score/[domain], the leaderboard, the sitemap) already filter out flow_version='v2' rows.
     */
    post: operations["scanDomainV2"];
  };
  "/api/scan/stream": {
    /**
     * Stream an agent-readiness scan as Server-Sent Events
     * @description Runs a full agent-readiness scan on the given URL and streams progress as text/event-stream. Accepts a domain, MCP server URL, or MCP App URL (server that supports the MCP Apps extension `io.modelcontextprotocol/ui`) - the server auto-detects which kind of input was provided and selects the appropriate check set. Catalog-style listing pages are folded into the `mcp` kind by classifying the first validated embedded MCP URL. The stream emits a `kind_detecting` event immediately after the cheap reachability probe, followed by exactly one `kind_detected` event with payload `{ kind: 'domain' | 'mcp' | 'mcp-app', mcpUrl?: string, embeddedMcpUrls?: string[], hint?: string }` once URL-kind detection resolves. Subsequent events include `scan_init`, `layer_start`, `check_start`, `check_complete`, `layer_complete`, and finally `scan_complete` whose payload mirrors the ScanResult schema (including the optional `urlKind` field indicating the detected kind). The same freshness gate as POST /api/scan applies: a hit is a `kind_detected` frame followed by the terminal `scan_complete` event, rather than a full run. Rate limited two ways: 10 requests per minute per IP (burst) and a durable daily scan budget shared with the other scan entry points - both return 429 with a Retry-After header.
     */
    get: operations["scanDomainStream"];
  };
  "/api/score/{domain}": {
    /**
     * Get cached score for a domain
     * @description Returns the most recent cached scan result for the given domain. Read-only: never triggers a scan. On miss (404) or when the previous scan got stuck mid-flight (200 with `analysisStatus: "stuck"`), the response carries a structured `next_action` envelope pointing at `POST /api/scan` so agent callers have a machine-parseable next step. Successful responses are cached for 1 hour; stuck, 404, and ephemeral (disposable, `urlKind: "ephemeral"`) responses are uncached (`Cache-Control: no-store`) so a successful re-scan is observable immediately and a deleted disposable row is never served from cache. Rate limited to 10 requests per minute per IP - returns 429 if exceeded.
     */
    get: operations["getScore"];
  };
  "/api/badge/{domain}": {
    /**
     * Get SVG badge for a domain
     * @description Returns an SVG badge showing the domain's ora score and grade. Embed in READMEs or websites. Cached for 1 hour.
     */
    get: operations["getBadge"];
  };
  "/api/discover": {
    /**
     * Discover agent-ready products by intent
     * @description Find the most agent-ready products for a given need. Describe what you're looking for and get products ranked by agent-readiness score. Cached for 5 minutes.
     */
    get: operations["discoverProducts"];
  };
  "/api/feedback/check": {
    /**
     * Report an issue with a specific check result
     * @description Submit feedback about an inaccurate check result. Accepts both human and agent submissions. Agent submissions require HATCHA verification. Check state (score, status, details) is snapshotted server-side from the latest scan.
     */
    post: operations["reportCheckIssue"];
  };
  "/api/contact": {
    /**
     * Send a message to the ora team
     * @description Submit a contact-form message. Open endpoint - no authentication required. Sends an inquiry email to the ora team and an auto-responder to the submitter. Rate limited to 3 submissions per IP per 10 minutes. Agents are welcome to use this endpoint, though email is the simpler path for most cases.
     */
    post: operations["submitContactInquiry"];
  };
  "/api/ard/search": {
    /**
     * Search agentic resources (Agentic Resource Discovery)
     * @description Runs an Agentic Resource Discovery (ARD) search over ora's catalog of agent-ready resources. Returns resources ranked by relevance to a free-text query, with optional field filters and federation control. The per-result `score` is a readiness-weighted relevance score (0-100): match quality for the query (dominant), multiplied by a 0.6-1.0 factor from the domain's agent-readiness. It is distinct from the raw agent-readiness score returned by POST /api/scan and GET /api/score/{domain}. Rate limited to 30 requests per minute per IP - returns 429 if exceeded.
     */
    post: operations["ardSearch"];
  };
  "/api/ard/explore": {
    /**
     * Faceted exploration of agentic resources (Agentic Resource Discovery)
     * @description Returns facet aggregations (counts per field value) for an Agentic Resource Discovery (ARD) result set. Use this to build filter UIs over the ARD catalog. An optional query narrows the set before faceting. Rate limited to 30 requests per minute per IP - returns 429 if exceeded.
     */
    post: operations["ardExplore"];
  };
  "/api/ard/agents": {
    /**
     * List discoverable agentic resources (Agentic Resource Discovery)
     * @description Returns a paginated listing of the agentic resources ora publishes for Agentic Resource Discovery (ARD). Deterministic browsing via the spec EBNF `filter` expression and `orderBy`. Rate limited to 60 requests per minute per IP - returns 429 if exceeded.
     */
    get: operations["ardListAgents"];
  };
  "/api/ard/attestation/{domain}": {
    /**
     * Get a signed scorecard attestation for a domain (Agentic Resource Discovery)
     * @description Returns an Agentic Resource Discovery (ARD) scorecard attestation for the given domain. When ora has an attestation signing key configured, the payload is returned as an EdDSA detached JWS that verifies against the public JWK set at GET /api/ard/jwks (also served at /.well-known/jwks.json); without a configured key the attestation is returned unsigned. Rate limited to 60 requests per minute per IP - returns 429 if exceeded.
     */
    get: operations["ardGetAttestation"];
  };
  "/api/ard": {
    /**
     * Get the ARD registry service descriptor (Agentic Resource Discovery)
     * @description Returns the application/ai-registry+json service descriptor at ora's registry base URL - the document ora advertises in its AI Catalog's application/ai-registry+json entry. Names the live search/explore/agents/catalog endpoints, the pageToken pagination model (max page size 100), the filter/orderBy/facet fields, the served media types, and the federation modes. A crawler that ingests /.well-known/ai-catalog.json and follows the registry entry lands here.
     */
    get: operations["ardGetRegistryDescriptor"];
  };
  "/api/ard/catalog": {
    /**
     * Get ora's AI Catalog manifest (Agentic Resource Discovery)
     * @description Returns ora's AI Catalog manifest for Agentic Resource Discovery (ARD). Also served at /.well-known/ai-catalog.json via a rewrite, and advertised by a Link header with rel="ai-catalog" on the homepage.
     */
    get: operations["ardGetCatalog"];
  };
  "/api/ard/catalog.json": {
    /**
     * Get the full ARD catalog dump (Agentic Resource Discovery)
     * @description Returns every indexed Agentic Resource Discovery (ARD) entry - ora's own products, their detected MCP server / skill resources, and crawled external catalogs - in one uncapped document { version, generatedAt, publisher, data }, for bulk ingest without paging through /api/ard/agents. Shares the same index pipeline as /agents and /explore. Sends Access-Control-Allow-Origin: * and is CDN-cached. Rate limited to 60 requests per minute per IP - returns 429 if exceeded. Also reachable at directory.ora.ai/api.json.
     */
    get: operations["ardGetCatalogDump"];
  };
  "/api/ard/jwks": {
    /**
     * Get the public JWK set for verifying signed attestations (Agentic Resource Discovery)
     * @description Returns the public JSON Web Key Set used to verify ora's signed Agentic Resource Discovery (ARD) scorecard attestations. Also served at /.well-known/jwks.json via a rewrite.
     */
    get: operations["ardGetJwks"];
  };
  "/api/journey/runs": {
    /**
     * Run an agent journey against a domain
     * @description Triggers a real agent run: an AI agent (harness + model) attempts a curated task (an intent) against the given domain, and ora records the trajectory and derives insights. Two-step flow: this endpoint returns fast with a run record whose `stream_url` serves the live trajectory as Server-Sent Events; poll GET /api/journey/runs/{id} instead if you do not want the stream. Only curated intents run publicly (see GET /api/journey/intents) - the server derives the actual agent prompt from the intent id, so no free-text prompt can reach the engine. Only publicly runnable agents are accepted (see GET /api/journey/agents). Unknown body fields are rejected (strict schema). Rate limited three ways: a 20-per-minute burst cap per IP; a per-target cap of 5 runs per rolling 24h per (domain, intent, harness, model) - when a target is over the cap the response is HTTP 200 with the most recent stored run for that target (`rate_limited: true`) plus Retry-After, the freshness analog: a denied trigger still returns the newest result and consumes nothing; and a durable per-caller cap of 10 runs per rolling 24h per IP - exceeded, that one is a real 429 with `retry_after_ms` (there is no cached result to serve for a caller). Successful and capped responses carry X-RateLimit-Limit / X-RateLimit-Remaining / X-RateLimit-Reset headers for the per-target window. Note: journey insights use the 5-layer journey taxonomy (discovery, identity, access, payments, experience), deliberately distinct from the 4 scoring layers of the audit report (POST /api/scan) - never map between the two.
     */
    post: operations["createJourneyRun"];
  };
  "/api/journey/runs/{id}": {
    /**
     * Get a journey run record (non-streaming)
     * @description Returns the run record by id. While the run executes, `status` is 'running' - open the stream or poll. Once finished and persisted, `status` is 'succeeded' and the body carries `verdict`, `step_count` (billable steps - the same counter run pricing uses), and the full `result` (trajectory + insight + signals). `status` 'failed' is terminal with no result. `result` is present iff status is 'succeeded'.
     */
    get: operations["getJourneyRun"];
  };
  "/api/journey/runs/{id}/stream": {
    /**
     * Stream a journey run's trajectory as Server-Sent Events
     * @description The live trajectory SSE for a run created by POST /api/journey/runs. Event names (stable): `run_id` ({ run_id }), then progressive `trajectory` frames (cumulative snapshots, each a { steps, ... } object of JourneyTrajectoryStep items), optionally `processing` ({ message }) while insights generate, and finally exactly one of `result` (a JourneyRunResult) or `error` ({ message }). Reopening the stream of a finished run replays the stored result instead of re-executing the agent (pass ?replay=1 for paced frames, plus &quick=1 to skip the startup delay); a plain reopen renders the finished journey in one frame. Long-lived: a live run can hold the connection for many minutes (function ceiling 800s). Rate limited 20 per minute per IP.
     */
    get: operations["streamJourneyRun"];
  };
  "/api/journey/intents": {
    /**
     * List the curated journey intents
     * @description The curated tasks a public journey run can execute. Each entry carries the stable `id` (what POST /api/journey/runs takes), a short `label`, a one-line `hint`, and the user-facing `template` phrasing. Cached statically.
     */
    get: operations["listJourneyIntents"];
  };
  "/api/journey/agents": {
    /**
     * List the publicly runnable journey agents
     * @description The agents an anonymous POST /api/journey/runs accepts - every listed (harness, model) pair is publicly runnable. Entries carry display metadata (label, variant, brand, blurb) plus the wire fields `harness` and `model`. Cached statically.
     */
    get: operations["listJourneyAgents"];
  };
  "/api/feedback/{domain}": {
    /**
     * Get agent feedback for a product
     * @description Returns feedback submitted by AI agents about their experience using a product. Includes aggregate stats and individual reviews.
     */
    get: operations["getAgentFeedback"];
  };
}

export type webhooks = Record<string, never>;

export interface components {
  schemas: {
    AuditCheck: {
      /** @description Stable check identifier (e.g. 'api-error-model'). Route fixes and dedupe by this - names are for display, ids are stable. */
      id: string;
      /** @description Human-readable check title */
      name: string;
      /** @description One of: pass | fail | warning | error | pending | na. Act on 'fail'/'warning'; skip 'na'; 'pending' means the scan has not finished. */
      status: string;
      /** @description Points earned WITHIN this layer. Not 0-100 score points - layers are normalized to a weight before they count, so do NOT read maxScore-score as score uplift. Use estScoreGain for that. */
      score: number;
      /** @description Points available within this layer for this check (the within-layer denominator, not the 0-100 scale). */
      maxScore: number;
      /** @description Estimated points this fix would add to the overall 0-100 score, already normalized to the layer weight. THIS is the uplift signal - rank fixes by it. Present on actionable (fail/warning) checks; an estimate, not exact. */
      estScoreGain?: number;
      /** @description Upside-only check: passing it raises the score, failing it never lowers the score. Build it only if the surface genuinely exists. */
      bonus?: boolean;
      /** @description verified (counts toward the 0-100 score) or emerging (forward-looking, excluded from the denominator - a lower priority) */
      maturity?: string;
      /** @description How strongly ora expects this check: required (the baseline every product is measured against), recommended (scored, outside the baseline), or emerging (excluded from the score). Display metadata - rank fixes by estScoreGain, not by tier. */
      tier?: string;
      /** @description Canonical spec / standard URL this check evaluates against, when one exists. Advisory - the link may change on any release. */
      specUrl?: string;
      /** @description When the check ran against a specific MCP server within a multi-MCP bundle: that server's kind (currently product | docs | other; advisory - new kinds may appear on any release). Absent for non-MCP checks and single-MCP scans. */
      mcpKind?: string;
      /** @description The URL of the MCP server this check scored against. Present only alongside mcpKind. */
      mcpUrl?: string;
      /** @description What the scan observed for this check */
      details?: string;
      /** @description Concrete fix that would make this check pass. The primary thing to act on. */
      recommendation?: string;
      /** @description Why this check does not apply to this product - it is skipped, not a deduction */
      naReason?: string;
    };
    AuditLayer: {
      /** @description Layer id: discovery | accessibility | usability | payments (historical scans may carry retired ids) */
      id: string;
      name: string;
      score: number;
      maxScore: number;
      checks: ({
          /** @description Stable check identifier (e.g. 'api-error-model'). Route fixes and dedupe by this - names are for display, ids are stable. */
          id: string;
          /** @description Human-readable check title */
          name: string;
          /** @description One of: pass | fail | warning | error | pending | na. Act on 'fail'/'warning'; skip 'na'; 'pending' means the scan has not finished. */
          status: string;
          /** @description Points earned WITHIN this layer. Not 0-100 score points - layers are normalized to a weight before they count, so do NOT read maxScore-score as score uplift. Use estScoreGain for that. */
          score: number;
          /** @description Points available within this layer for this check (the within-layer denominator, not the 0-100 scale). */
          maxScore: number;
          /** @description Estimated points this fix would add to the overall 0-100 score, already normalized to the layer weight. THIS is the uplift signal - rank fixes by it. Present on actionable (fail/warning) checks; an estimate, not exact. */
          estScoreGain?: number;
          /** @description Upside-only check: passing it raises the score, failing it never lowers the score. Build it only if the surface genuinely exists. */
          bonus?: boolean;
          /** @description verified (counts toward the 0-100 score) or emerging (forward-looking, excluded from the denominator - a lower priority) */
          maturity?: string;
          /** @description How strongly ora expects this check: required (the baseline every product is measured against), recommended (scored, outside the baseline), or emerging (excluded from the score). Display metadata - rank fixes by estScoreGain, not by tier. */
          tier?: string;
          /** @description Canonical spec / standard URL this check evaluates against, when one exists. Advisory - the link may change on any release. */
          specUrl?: string;
          /** @description When the check ran against a specific MCP server within a multi-MCP bundle: that server's kind (currently product | docs | other; advisory - new kinds may appear on any release). Absent for non-MCP checks and single-MCP scans. */
          mcpKind?: string;
          /** @description The URL of the MCP server this check scored against. Present only alongside mcpKind. */
          mcpUrl?: string;
          /** @description What the scan observed for this check */
          details?: string;
          /** @description Concrete fix that would make this check pass. The primary thing to act on. */
          recommendation?: string;
          /** @description Why this check does not apply to this product - it is skipped, not a deduction */
          naReason?: string;
        })[];
    };
    AuditScanResult: {
      domain: string;
      name: string;
      score: number;
      /** @enum {number} */
      scoreMax: 100;
      grade: string;
      gradeColor: string;
      ctaMessage: string | null;
      scannedAt: string;
      durationMs: number | null;
      /**
       * @description complete = all checks resolved; partial/stuck = still running - re-scan before acting
       * @enum {string}
       */
      analysisStatus?: "complete" | "partial" | "stuck";
      /** @description Ids of checks still resolving (empty/absent when analysisStatus is complete) */
      pendingChecks?: string[];
      layers: ({
          /** @description Layer id: discovery | accessibility | usability | payments (historical scans may carry retired ids) */
          id: string;
          name: string;
          score: number;
          maxScore: number;
          checks: ({
              /** @description Stable check identifier (e.g. 'api-error-model'). Route fixes and dedupe by this - names are for display, ids are stable. */
              id: string;
              /** @description Human-readable check title */
              name: string;
              /** @description One of: pass | fail | warning | error | pending | na. Act on 'fail'/'warning'; skip 'na'; 'pending' means the scan has not finished. */
              status: string;
              /** @description Points earned WITHIN this layer. Not 0-100 score points - layers are normalized to a weight before they count, so do NOT read maxScore-score as score uplift. Use estScoreGain for that. */
              score: number;
              /** @description Points available within this layer for this check (the within-layer denominator, not the 0-100 scale). */
              maxScore: number;
              /** @description Estimated points this fix would add to the overall 0-100 score, already normalized to the layer weight. THIS is the uplift signal - rank fixes by it. Present on actionable (fail/warning) checks; an estimate, not exact. */
              estScoreGain?: number;
              /** @description Upside-only check: passing it raises the score, failing it never lowers the score. Build it only if the surface genuinely exists. */
              bonus?: boolean;
              /** @description verified (counts toward the 0-100 score) or emerging (forward-looking, excluded from the denominator - a lower priority) */
              maturity?: string;
              /** @description How strongly ora expects this check: required (the baseline every product is measured against), recommended (scored, outside the baseline), or emerging (excluded from the score). Display metadata - rank fixes by estScoreGain, not by tier. */
              tier?: string;
              /** @description Canonical spec / standard URL this check evaluates against, when one exists. Advisory - the link may change on any release. */
              specUrl?: string;
              /** @description When the check ran against a specific MCP server within a multi-MCP bundle: that server's kind (currently product | docs | other; advisory - new kinds may appear on any release). Absent for non-MCP checks and single-MCP scans. */
              mcpKind?: string;
              /** @description The URL of the MCP server this check scored against. Present only alongside mcpKind. */
              mcpUrl?: string;
              /** @description What the scan observed for this check */
              details?: string;
              /** @description Concrete fix that would make this check pass. The primary thing to act on. */
              recommendation?: string;
              /** @description Why this check does not apply to this product - it is skipped, not a deduction */
              naReason?: string;
            })[];
        })[];
      /** @description Actionable (fail/warning) checks ranked by ora: non-bonus first, then estimated uplift descending, capped at 6. Render verbatim - do not re-rank. */
      topFixes: {
          /** @description Check id of the fix (stable) - matches a check in layers */
          id: string;
          /** @description Id of the layer the check belongs to */
          layerId: string;
          /** @description Human-readable check title */
          name: string;
          /** @description Estimated points this fix would add to the overall 0-100 score. Absent when the uplift cannot be estimated. */
          estScoreGain?: number;
          /** @description Upside-only check: build it only if the surface genuinely exists. Bonus fixes rank after non-bonus ones. */
          bonus: boolean;
          /** @description Concrete fix that would make this check pass */
          recommendation?: string;
        }[];
      /** @description Canonical ora.ai deep link for the domain */
      url: string;
      generatedAt: string;
      /** @enum {string} */
      source: "ora.ai";
      /**
       * @description The contract version this payload conforms to. SemVer: a major means a stable field or check id was removed, renamed, or changed meaning. See docs/api.md -> Contract and versioning.
       * @enum {string}
       */
      contractVersion: "1.11.0";
      /**
       * @description Present only when this body is a stored result served by the freshness gate instead of a fresh scan. Absent on a live scan.
       * @enum {boolean}
       */
      servedFromCache?: true;
      /** @description Age of the served stored result in seconds (also sent as the Age response header). Present with servedFromCache. */
      resultAgeSeconds?: number;
      /**
       * @description Present when an MCP-family scan short-circuited because the server's handshake demanded credentials (401/403). Score 0 with empty layers then means 'could not evaluate', NOT 'failed everything' - treat the target as unscored rather than failing a gate on it.
       * @enum {boolean}
       */
      mcpAuthRequired?: true;
      /**
       * @description How the scanned input was classified and stored. 'ephemeral' = disposable result (tunnel host, or ephemeral: true) that is excluded from rankings and deleted after a few days. Absent on older stored results.
       * @enum {string}
       */
      urlKind?: "domain" | "mcp" | "mcp-app" | "ephemeral";
      /** @description Canonical market category ora classified the domain into (e.g. 'Infrastructure & DevOps'). Advisory; absent when the domain is unclassified. */
      category?: string;
      /** @description One-sentence natural-language verdict on the domain's agent-readiness, generated after analysis completes. Advisory; absent on partial results and on older stored results. */
      agenticSummary?: string;
      /** @description The URL the scan actually fetched after following redirects. Distinct from `url`, which is the canonical ora.ai deep link for the domain. */
      finalUrl?: string;
      /** @description Present only when the scan is stuck (partial for over 30 minutes; the worker likely failed) or on the score route's 404 miss - the HTTP request that recovers the score. A plain partial resolves on its own: poll the 202's Location URL instead of re-scanning. */
      nextAction?: {
        /** @enum {string} */
        kind: "scan";
        /** @enum {string} */
        method: "POST";
        /** @enum {string} */
        endpoint: "/api/scan";
        body: {
          url: string;
        };
        /** @description Why this request is the recommended next step */
        reason: string;
      };
      /** @description Experimental (no stability guarantee): competitive context appended when the request carries ?competitors=1. Null when the domain is unranked or the leaderboard read failed. Shape may change on any release. */
      competitors?: {
        [key: string]: unknown;
      } | null;
    };
    AuditScoreResult: {
      domain: string;
      name: string;
      score: number;
      /** @enum {number} */
      scoreMax: 100;
      grade: string;
      gradeColor: string;
      scannedAt: string | null;
      /**
       * @description complete = all checks resolved; partial/stuck = still running - re-scan before acting
       * @enum {string}
       */
      analysisStatus?: "complete" | "partial" | "stuck";
      /** @description Ids of checks still resolving (empty/absent when analysisStatus is complete) */
      pendingChecks?: string[];
      layers: ({
          /** @description Layer id: discovery | accessibility | usability | payments (historical scans may carry retired ids) */
          id: string;
          name: string;
          score: number;
          maxScore: number;
          checks: ({
              /** @description Stable check identifier (e.g. 'api-error-model'). Route fixes and dedupe by this - names are for display, ids are stable. */
              id: string;
              /** @description Human-readable check title */
              name: string;
              /** @description One of: pass | fail | warning | error | pending | na. Act on 'fail'/'warning'; skip 'na'; 'pending' means the scan has not finished. */
              status: string;
              /** @description Points earned WITHIN this layer. Not 0-100 score points - layers are normalized to a weight before they count, so do NOT read maxScore-score as score uplift. Use estScoreGain for that. */
              score: number;
              /** @description Points available within this layer for this check (the within-layer denominator, not the 0-100 scale). */
              maxScore: number;
              /** @description Estimated points this fix would add to the overall 0-100 score, already normalized to the layer weight. THIS is the uplift signal - rank fixes by it. Present on actionable (fail/warning) checks; an estimate, not exact. */
              estScoreGain?: number;
              /** @description Upside-only check: passing it raises the score, failing it never lowers the score. Build it only if the surface genuinely exists. */
              bonus?: boolean;
              /** @description verified (counts toward the 0-100 score) or emerging (forward-looking, excluded from the denominator - a lower priority) */
              maturity?: string;
              /** @description How strongly ora expects this check: required (the baseline every product is measured against), recommended (scored, outside the baseline), or emerging (excluded from the score). Display metadata - rank fixes by estScoreGain, not by tier. */
              tier?: string;
              /** @description Canonical spec / standard URL this check evaluates against, when one exists. Advisory - the link may change on any release. */
              specUrl?: string;
              /** @description When the check ran against a specific MCP server within a multi-MCP bundle: that server's kind (currently product | docs | other; advisory - new kinds may appear on any release). Absent for non-MCP checks and single-MCP scans. */
              mcpKind?: string;
              /** @description The URL of the MCP server this check scored against. Present only alongside mcpKind. */
              mcpUrl?: string;
              /** @description What the scan observed for this check */
              details?: string;
              /** @description Concrete fix that would make this check pass. The primary thing to act on. */
              recommendation?: string;
              /** @description Why this check does not apply to this product - it is skipped, not a deduction */
              naReason?: string;
            })[];
        })[];
      /** @description Actionable (fail/warning) checks ranked by ora: non-bonus first, then estimated uplift descending, capped at 6. Render verbatim - do not re-rank. */
      topFixes: {
          /** @description Check id of the fix (stable) - matches a check in layers */
          id: string;
          /** @description Id of the layer the check belongs to */
          layerId: string;
          /** @description Human-readable check title */
          name: string;
          /** @description Estimated points this fix would add to the overall 0-100 score. Absent when the uplift cannot be estimated. */
          estScoreGain?: number;
          /** @description Upside-only check: build it only if the surface genuinely exists. Bonus fixes rank after non-bonus ones. */
          bonus: boolean;
          /** @description Concrete fix that would make this check pass */
          recommendation?: string;
        }[];
      /** @description Canonical ora.ai deep link for the domain */
      url: string;
      generatedAt: string;
      /** @enum {string} */
      source: "ora.ai";
      /**
       * @description The contract version this payload conforms to. SemVer: a major means a stable field or check id was removed, renamed, or changed meaning. See docs/api.md -> Contract and versioning.
       * @enum {string}
       */
      contractVersion: "1.11.0";
      /** @description Wall-clock duration of the scan that produced this stored result */
      durationMs: number | null;
      /**
       * @description Present when an MCP-family scan short-circuited because the server's handshake demanded credentials (401/403). Score 0 with empty layers then means 'could not evaluate', NOT 'failed everything' - treat the target as unscored rather than failing a gate on it.
       * @enum {boolean}
       */
      mcpAuthRequired?: true;
      /**
       * @description How the scanned input was classified and stored. 'ephemeral' = disposable result (tunnel host, or ephemeral: true) that is excluded from rankings and deleted after a few days. Absent on older stored results.
       * @enum {string}
       */
      urlKind?: "domain" | "mcp" | "mcp-app" | "ephemeral";
      /** @description Canonical market category ora classified the domain into (e.g. 'Infrastructure & DevOps'). Advisory; absent when the domain is unclassified. */
      category?: string;
      /** @description One-sentence natural-language verdict on the domain's agent-readiness, generated after analysis completes. Advisory; absent on partial results and on older stored results. */
      agenticSummary?: string;
      /** @description The URL the scan actually fetched after following redirects. Distinct from `url`, which is the canonical ora.ai deep link for the domain. */
      finalUrl?: string;
      /** @description Present only when the scan is stuck (partial for over 30 minutes; the worker likely failed) or on the score route's 404 miss - the HTTP request that recovers the score. A plain partial resolves on its own: poll the 202's Location URL instead of re-scanning. */
      nextAction?: {
        /** @enum {string} */
        kind: "scan";
        /** @enum {string} */
        method: "POST";
        /** @enum {string} */
        endpoint: "/api/scan";
        body: {
          url: string;
        };
        /** @description Why this request is the recommended next step */
        reason: string;
      };
      /** @description Experimental (no stability guarantee): competitive context appended when the request carries ?competitors=1. Null when the domain is unranked or the leaderboard read failed. Shape may change on any release. */
      competitors?: {
        [key: string]: unknown;
      } | null;
    };
    JourneyRun: {
      /** @description Run id - the handle for GET /api/journey/runs/{id} and the stream */
      id: string;
      /**
       * @description Lifecycle status. failed is terminal; a failed run has no result.
       * @enum {string}
       */
      status: "running" | "succeeded" | "failed";
      /** @description Curated intent id (see GET /api/journey/intents) */
      intent_id?: string;
      /** @description Target domain */
      domain?: string;
      /** @description The agent configuration that ran (or is running) */
      agent: {
        /** @description Agent harness wire name */
        harness: string;
        /** @description Model the harness drives */
        model: string;
      };
      started_at: string;
      finished_at?: string;
      /** @description SSE stream for this run: run_id -> trajectory -> processing -> result | error */
      stream_url: string;
      /**
       * @description Canonical success verdict, present once the run finished
       * @enum {string}
       */
      verdict?: "satisfied" | "partial" | "unsatisfied" | "not_gradable";
      /** @description Billable steps the run took (tool calls, excluding narration and filesystem ops), present once the run finished. This is the same counter run pricing uses. */
      step_count?: number;
      /** @description Journey/audit contract version (shared SemVer; see docs) */
      contractVersion: string;
    };
    JourneyCappedRun: {
      /** @description Run id - the handle for GET /api/journey/runs/{id} and the stream */
      id: string;
      /**
       * @description Lifecycle status. failed is terminal; a failed run has no result.
       * @enum {string}
       */
      status: "running" | "succeeded" | "failed";
      /** @description Curated intent id (see GET /api/journey/intents) */
      intent_id?: string;
      /** @description Target domain */
      domain?: string;
      /** @description The agent configuration that ran (or is running) */
      agent: {
        /** @description Agent harness wire name */
        harness: string;
        /** @description Model the harness drives */
        model: string;
      };
      started_at: string;
      finished_at?: string;
      /** @description SSE stream for this run: run_id -> trajectory -> processing -> result | error */
      stream_url: string;
      /**
       * @description Canonical success verdict, present once the run finished
       * @enum {string}
       */
      verdict?: "satisfied" | "partial" | "unsatisfied" | "not_gradable";
      /** @description Billable steps the run took (tool calls, excluding narration and filesystem ops), present once the run finished. This is the same counter run pricing uses. */
      step_count?: number;
      /** @description Journey/audit contract version (shared SemVer; see docs) */
      contractVersion: string;
      /**
       * @description Marks a per-target-capped response: this is the cached latest run, not a fresh one
       * @enum {boolean}
       */
      rate_limited: true;
      /** @description The per-target cap that was hit */
      limit: {
        /** @description Runs allowed per target per window */
        max: number;
        /** @description Window length in ms */
        window_ms: number;
      };
      /** @description Milliseconds until a per-target slot frees */
      retry_after_ms: number;
    };
    JourneyRunDetail: {
      /** @description Run id - the handle for GET /api/journey/runs/{id} and the stream */
      id: string;
      /**
       * @description Lifecycle status. failed is terminal; a failed run has no result.
       * @enum {string}
       */
      status: "running" | "succeeded" | "failed";
      /** @description Curated intent id (see GET /api/journey/intents) */
      intent_id?: string;
      /** @description Target domain */
      domain?: string;
      /** @description The agent configuration that ran (or is running) */
      agent: {
        /** @description Agent harness wire name */
        harness: string;
        /** @description Model the harness drives */
        model: string;
      };
      started_at: string;
      finished_at?: string;
      /** @description SSE stream for this run: run_id -> trajectory -> processing -> result | error */
      stream_url: string;
      /**
       * @description Canonical success verdict, present once the run finished
       * @enum {string}
       */
      verdict?: "satisfied" | "partial" | "unsatisfied" | "not_gradable";
      /** @description Billable steps the run took (tool calls, excluding narration and filesystem ops), present once the run finished. This is the same counter run pricing uses. */
      step_count?: number;
      /** @description Journey/audit contract version (shared SemVer; see docs) */
      contractVersion: string;
      /** @description The full run result. Present iff status is 'succeeded'. */
      result?: {
        /** @description Run id. Absent on legacy persisted runs. */
        run_id?: string;
        /** @description Curated intent id the run executed */
        intent_id?: string;
        /** @description Target domain */
        domain?: string;
        /** @description Agent harness: claude-agent-sdk | openai-agents | ash */
        harness?: string;
        /** @description Model the harness drove */
        model?: string;
        started_at?: string;
        finished_at?: string;
        /** @description Agent lifecycle outcome. Key 'did it work?' off verdict, not this. */
        outcome: string;
        /**
         * @description Canonical success verdict, judge-authoritative and always resolved (legacy runs included). THE field to key success on.
         * @enum {string}
         */
        verdict: "satisfied" | "partial" | "unsatisfied" | "not_gradable";
        /** @description The agent's final free-text answer. Experimental: raw model output that can reflect content fetched from the target site - may change or disappear without a major version. */
        agent_response?: string;
        /** @description Agent turns the run took */
        num_turns?: number;
        /** @description Wall-clock run duration */
        duration_ms?: number;
        /** @description Model spend for the run in USD. Experimental: tracks provider accounting and may change or disappear without a major version. */
        cost_usd?: number;
        /** @description Total input tokens the run consumed. Experimental. */
        input_tokens?: number;
        /** @description Total output tokens the run produced. Experimental. */
        output_tokens?: number;
        /** @description Prompt-cache read tokens. Experimental. */
        cache_read_tokens?: number;
        /** @description Prompt-cache write tokens. Experimental. */
        cache_write_tokens?: number;
        /** @description The full step tree the agent took */
        trajectory: {
          /** @description Cumulative step tree, in emission order */
          steps: ({
              /** @description Index in steps[] - stable node identifier */
              id: number;
              /** @description Agent turn this step belongs to */
              turn?: number;
              /**
               * @description tool_call = an action against the target; text = narrative reasoning between actions
               * @enum {string}
               */
              type: "tool_call" | "text";
              /** @description Index of the parent step in this same array (tree edge) */
              parent_id?: number;
              /** @description Action family: search | fetch | api_call | text | bash_fs | skill */
              action?: string;
              /** @description Concrete tool the harness invoked */
              tool?: string;
              /** @description Full URL the step targeted, when it targeted one */
              url?: string;
              /** @description Host of the targeted URL */
              url_host?: string;
              /** @description Path of the targeted URL */
              url_path?: string;
              /** @description Query string, on search steps */
              search_query?: string;
              /** @description direct = the agent navigated on its own; follow = it followed a link */
              source?: string;
              /** @description Relation of the target to the run's domain: exact | subdomain | external */
              anchor_relation?: string;
              /** @description Why the agent went here - the navigation-source attribution */
              attribution?: {
                /** @description How the agent found this step: prior_knowledge | web_search | previous_artifact | other */
                kind: string;
                /** @description Artifact kind this step was attributed to */
                artifact_kind?: string;
                /** @description Signal id backing the attribution, when one matched */
                signal_id?: string;
                /** @description Attribution method: heuristic | llm */
                method?: string;
                /** @description Attribution confidence: strong | weak */
                confidence?: string;
                /** @description The referring step, when attributed to a previous artifact */
                referrer?: {
                  /** @description Turn index of the referring step */
                  turn: number;
                  /** @description Parent step id - the tree edge the trajectory graph renders */
                  step_id?: number;
                  /** @description Artifact kind of the referring step (e.g. llms_txt, sitemap) */
                  artifact_kind?: string;
                  /** @description URL path of the referring step */
                  url_path?: string;
                };
              };
              /** @description Key of the artifact this step fetched, when recognised */
              artifact_key?: string;
              /** @description HTTP method of a fetch step */
              fetch_method?: string;
              /** @description HTTP status the step observed, once resolved */
              status?: number;
              /** @description Wall-clock duration of the step */
              duration_ms?: number;
              /** @description Engine-provided display label for the node, when present */
              label?: string;
              /** @description tool_call steps only: whether the call has resolved. False while the step is still in flight on a live stream. */
              completed?: boolean;
              /** @description Name of the invoked skill, on skill steps */
              skill_name?: string;
              /** @description text steps only: the narrative the agent emitted between actions. Advisory - raw model prose. */
              text?: string;
            })[];
          /** @description Compact fingerprint of the action sequence */
          action_sequence: string;
          /** @description Ids of agent-readiness signals the run observed */
          signals_observed: string[];
          /** @description How the run resolved friction, when classified (e.g. succeeded_natively, needs_human_bridge) */
          friction_outcome?: string;
          /** @description HTTP status distribution over the run's requests */
          status_profile?: {
            count_2xx: number;
            count_3xx: number;
            count_4xx: number;
            count_5xx: number;
            /** @description Step id of the first 4xx/5xx, when any */
            first_error_step?: number;
            /** @description Artifact kind of the first errored step */
            first_error_kind?: string;
          };
          /** @description Which discovery artifacts the agent probed for, and what it found */
          well_known_probes: ({
              /** @description Probed artifact (e.g. llms.txt, openapi.json) */
              artifact: string;
              signal_id?: string;
              /** @description root_file | well_known | api_spec */
              catalog: string;
              probed: boolean;
              fetched: boolean;
              attempt_count: number;
              first_status?: number;
              first_step_ord?: number;
              first_turn_index?: number;
              /** @description The agent looked for this artifact and it was absent */
              near_miss: boolean;
            })[];
          /** @description How the agent's visited paths were discovered */
          path_origin_distribution?: {
            prior_knowledge: number;
            web_search: number;
            previous_artifact: number;
            other: number;
          };
          /** @description Share of navigations that followed an on-page link rather than a guess */
          link_following_rate: number;
        };
        /** @description ora's generated read of the run */
        insight: {
          /** @description ora's generated one-paragraph read of the run */
          summary: string;
          /** @description Bullet observations backing the summary */
          key_observations: string[];
          generated_at: string;
          /** @description The journey layers this intent touched. NOTE: this is the journey taxonomy (5 layers), deliberately distinct from the audit report's 4 scoring layers - never map between the two. */
          journey_layers?: ("discovery" | "identity" | "access" | "payments" | "experience")[];
        };
        /** @description Flat per-run signal summary. Experimental; absent on legacy runs. */
        run_signals?: {
          /** @description run_signals contract version (3 = current) */
          version: number;
          /** @description Engine-classified intent category */
          intent_category: string;
          /** @description high | low */
          category_confidence: string;
          /** @description The independent judge's grade: satisfied | partial | unsatisfied */
          task_satisfied?: string;
          /** @description Engine-reconciled success verdict; prefer the top-level verdict field, which is always resolved */
          verdict?: string;
          /** @description Agent lifecycle outcome. Key 'did it work?' off the top-level verdict, not this. */
          outcome: string;
          friction_outcome?: string;
          bridge_classification?: string;
          first_action?: string;
          /** @description Where the agent went first */
          first_target?: {
            page_role: string;
            anchor_relation?: string;
            source?: string;
          };
          /** @description Engine's raw step count. Display step counts use the record's step_count (billable steps) instead. */
          steps_count: number;
          search_count: number;
          /** @description Whether the agent reached the run's domain at all */
          reached_anchor: boolean;
          link_following_rate: number;
          prior_knowledge_ratio: number;
          signals_observed: string[];
          /** @description Per page-role reach summary */
          page_reach: {
            [key: string]: {
              reached: boolean;
              first_turn: number;
              steps_to: number;
            };
          };
          /** @description Per artifact probe summary */
          artifacts: {
            [key: string]: {
              probed: boolean;
              fetched: boolean;
              first_turn?: number;
            };
          };
          /** @description Step ids on the success path, when the run succeeded */
          success_step_ids: number[];
          success_route: {
            artifacts: {
              [key: string]: string;
            };
            page_roles: {
              [key: string]: string;
            };
            closer?: {
              artifact_key?: string;
              page_role?: string;
            };
          };
          /** @description Journey-taxonomy layers (5), distinct from the audit report's 4 scoring layers */
          journey_layers: ("discovery" | "identity" | "access" | "payments" | "experience")[];
        };
      };
    };
    JourneyRunResult: {
      /** @description Run id. Absent on legacy persisted runs. */
      run_id?: string;
      /** @description Curated intent id the run executed */
      intent_id?: string;
      /** @description Target domain */
      domain?: string;
      /** @description Agent harness: claude-agent-sdk | openai-agents | ash */
      harness?: string;
      /** @description Model the harness drove */
      model?: string;
      started_at?: string;
      finished_at?: string;
      /** @description Agent lifecycle outcome. Key 'did it work?' off verdict, not this. */
      outcome: string;
      /**
       * @description Canonical success verdict, judge-authoritative and always resolved (legacy runs included). THE field to key success on.
       * @enum {string}
       */
      verdict: "satisfied" | "partial" | "unsatisfied" | "not_gradable";
      /** @description The agent's final free-text answer. Experimental: raw model output that can reflect content fetched from the target site - may change or disappear without a major version. */
      agent_response?: string;
      /** @description Agent turns the run took */
      num_turns?: number;
      /** @description Wall-clock run duration */
      duration_ms?: number;
      /** @description Model spend for the run in USD. Experimental: tracks provider accounting and may change or disappear without a major version. */
      cost_usd?: number;
      /** @description Total input tokens the run consumed. Experimental. */
      input_tokens?: number;
      /** @description Total output tokens the run produced. Experimental. */
      output_tokens?: number;
      /** @description Prompt-cache read tokens. Experimental. */
      cache_read_tokens?: number;
      /** @description Prompt-cache write tokens. Experimental. */
      cache_write_tokens?: number;
      /** @description The full step tree the agent took */
      trajectory: {
        /** @description Cumulative step tree, in emission order */
        steps: ({
            /** @description Index in steps[] - stable node identifier */
            id: number;
            /** @description Agent turn this step belongs to */
            turn?: number;
            /**
             * @description tool_call = an action against the target; text = narrative reasoning between actions
             * @enum {string}
             */
            type: "tool_call" | "text";
            /** @description Index of the parent step in this same array (tree edge) */
            parent_id?: number;
            /** @description Action family: search | fetch | api_call | text | bash_fs | skill */
            action?: string;
            /** @description Concrete tool the harness invoked */
            tool?: string;
            /** @description Full URL the step targeted, when it targeted one */
            url?: string;
            /** @description Host of the targeted URL */
            url_host?: string;
            /** @description Path of the targeted URL */
            url_path?: string;
            /** @description Query string, on search steps */
            search_query?: string;
            /** @description direct = the agent navigated on its own; follow = it followed a link */
            source?: string;
            /** @description Relation of the target to the run's domain: exact | subdomain | external */
            anchor_relation?: string;
            /** @description Why the agent went here - the navigation-source attribution */
            attribution?: {
              /** @description How the agent found this step: prior_knowledge | web_search | previous_artifact | other */
              kind: string;
              /** @description Artifact kind this step was attributed to */
              artifact_kind?: string;
              /** @description Signal id backing the attribution, when one matched */
              signal_id?: string;
              /** @description Attribution method: heuristic | llm */
              method?: string;
              /** @description Attribution confidence: strong | weak */
              confidence?: string;
              /** @description The referring step, when attributed to a previous artifact */
              referrer?: {
                /** @description Turn index of the referring step */
                turn: number;
                /** @description Parent step id - the tree edge the trajectory graph renders */
                step_id?: number;
                /** @description Artifact kind of the referring step (e.g. llms_txt, sitemap) */
                artifact_kind?: string;
                /** @description URL path of the referring step */
                url_path?: string;
              };
            };
            /** @description Key of the artifact this step fetched, when recognised */
            artifact_key?: string;
            /** @description HTTP method of a fetch step */
            fetch_method?: string;
            /** @description HTTP status the step observed, once resolved */
            status?: number;
            /** @description Wall-clock duration of the step */
            duration_ms?: number;
            /** @description Engine-provided display label for the node, when present */
            label?: string;
            /** @description tool_call steps only: whether the call has resolved. False while the step is still in flight on a live stream. */
            completed?: boolean;
            /** @description Name of the invoked skill, on skill steps */
            skill_name?: string;
            /** @description text steps only: the narrative the agent emitted between actions. Advisory - raw model prose. */
            text?: string;
          })[];
        /** @description Compact fingerprint of the action sequence */
        action_sequence: string;
        /** @description Ids of agent-readiness signals the run observed */
        signals_observed: string[];
        /** @description How the run resolved friction, when classified (e.g. succeeded_natively, needs_human_bridge) */
        friction_outcome?: string;
        /** @description HTTP status distribution over the run's requests */
        status_profile?: {
          count_2xx: number;
          count_3xx: number;
          count_4xx: number;
          count_5xx: number;
          /** @description Step id of the first 4xx/5xx, when any */
          first_error_step?: number;
          /** @description Artifact kind of the first errored step */
          first_error_kind?: string;
        };
        /** @description Which discovery artifacts the agent probed for, and what it found */
        well_known_probes: ({
            /** @description Probed artifact (e.g. llms.txt, openapi.json) */
            artifact: string;
            signal_id?: string;
            /** @description root_file | well_known | api_spec */
            catalog: string;
            probed: boolean;
            fetched: boolean;
            attempt_count: number;
            first_status?: number;
            first_step_ord?: number;
            first_turn_index?: number;
            /** @description The agent looked for this artifact and it was absent */
            near_miss: boolean;
          })[];
        /** @description How the agent's visited paths were discovered */
        path_origin_distribution?: {
          prior_knowledge: number;
          web_search: number;
          previous_artifact: number;
          other: number;
        };
        /** @description Share of navigations that followed an on-page link rather than a guess */
        link_following_rate: number;
      };
      /** @description ora's generated read of the run */
      insight: {
        /** @description ora's generated one-paragraph read of the run */
        summary: string;
        /** @description Bullet observations backing the summary */
        key_observations: string[];
        generated_at: string;
        /** @description The journey layers this intent touched. NOTE: this is the journey taxonomy (5 layers), deliberately distinct from the audit report's 4 scoring layers - never map between the two. */
        journey_layers?: ("discovery" | "identity" | "access" | "payments" | "experience")[];
      };
      /** @description Flat per-run signal summary. Experimental; absent on legacy runs. */
      run_signals?: {
        /** @description run_signals contract version (3 = current) */
        version: number;
        /** @description Engine-classified intent category */
        intent_category: string;
        /** @description high | low */
        category_confidence: string;
        /** @description The independent judge's grade: satisfied | partial | unsatisfied */
        task_satisfied?: string;
        /** @description Engine-reconciled success verdict; prefer the top-level verdict field, which is always resolved */
        verdict?: string;
        /** @description Agent lifecycle outcome. Key 'did it work?' off the top-level verdict, not this. */
        outcome: string;
        friction_outcome?: string;
        bridge_classification?: string;
        first_action?: string;
        /** @description Where the agent went first */
        first_target?: {
          page_role: string;
          anchor_relation?: string;
          source?: string;
        };
        /** @description Engine's raw step count. Display step counts use the record's step_count (billable steps) instead. */
        steps_count: number;
        search_count: number;
        /** @description Whether the agent reached the run's domain at all */
        reached_anchor: boolean;
        link_following_rate: number;
        prior_knowledge_ratio: number;
        signals_observed: string[];
        /** @description Per page-role reach summary */
        page_reach: {
          [key: string]: {
            reached: boolean;
            first_turn: number;
            steps_to: number;
          };
        };
        /** @description Per artifact probe summary */
        artifacts: {
          [key: string]: {
            probed: boolean;
            fetched: boolean;
            first_turn?: number;
          };
        };
        /** @description Step ids on the success path, when the run succeeded */
        success_step_ids: number[];
        success_route: {
          artifacts: {
            [key: string]: string;
          };
          page_roles: {
            [key: string]: string;
          };
          closer?: {
            artifact_key?: string;
            page_role?: string;
          };
        };
        /** @description Journey-taxonomy layers (5), distinct from the audit report's 4 scoring layers */
        journey_layers: ("discovery" | "identity" | "access" | "payments" | "experience")[];
      };
    };
    JourneyTrajectoryStep: {
      /** @description Index in steps[] - stable node identifier */
      id: number;
      /** @description Agent turn this step belongs to */
      turn?: number;
      /**
       * @description tool_call = an action against the target; text = narrative reasoning between actions
       * @enum {string}
       */
      type: "tool_call" | "text";
      /** @description Index of the parent step in this same array (tree edge) */
      parent_id?: number;
      /** @description Action family: search | fetch | api_call | text | bash_fs | skill */
      action?: string;
      /** @description Concrete tool the harness invoked */
      tool?: string;
      /** @description Full URL the step targeted, when it targeted one */
      url?: string;
      /** @description Host of the targeted URL */
      url_host?: string;
      /** @description Path of the targeted URL */
      url_path?: string;
      /** @description Query string, on search steps */
      search_query?: string;
      /** @description direct = the agent navigated on its own; follow = it followed a link */
      source?: string;
      /** @description Relation of the target to the run's domain: exact | subdomain | external */
      anchor_relation?: string;
      /** @description Why the agent went here - the navigation-source attribution */
      attribution?: {
        /** @description How the agent found this step: prior_knowledge | web_search | previous_artifact | other */
        kind: string;
        /** @description Artifact kind this step was attributed to */
        artifact_kind?: string;
        /** @description Signal id backing the attribution, when one matched */
        signal_id?: string;
        /** @description Attribution method: heuristic | llm */
        method?: string;
        /** @description Attribution confidence: strong | weak */
        confidence?: string;
        /** @description The referring step, when attributed to a previous artifact */
        referrer?: {
          /** @description Turn index of the referring step */
          turn: number;
          /** @description Parent step id - the tree edge the trajectory graph renders */
          step_id?: number;
          /** @description Artifact kind of the referring step (e.g. llms_txt, sitemap) */
          artifact_kind?: string;
          /** @description URL path of the referring step */
          url_path?: string;
        };
      };
      /** @description Key of the artifact this step fetched, when recognised */
      artifact_key?: string;
      /** @description HTTP method of a fetch step */
      fetch_method?: string;
      /** @description HTTP status the step observed, once resolved */
      status?: number;
      /** @description Wall-clock duration of the step */
      duration_ms?: number;
      /** @description Engine-provided display label for the node, when present */
      label?: string;
      /** @description tool_call steps only: whether the call has resolved. False while the step is still in flight on a live stream. */
      completed?: boolean;
      /** @description Name of the invoked skill, on skill steps */
      skill_name?: string;
      /** @description text steps only: the narrative the agent emitted between actions. Advisory - raw model prose. */
      text?: string;
    };
    /** @description The default response body of POST /api/scan and GET /api/score/{domain}. Fields not listed below may be present: they are ora internals, are not part of the contract, and may change or disappear in any release without a major version bump. Pass `?format=audit` for the versioned, fully documented shape (AuditScanResult / AuditScoreResult). */
    ScanResult: {
      /** @description The scanned domain (pre-redirect). Compare with new URL(finalUrl).hostname to detect cross-domain redirects. */
      domain?: string;
      /** @description The normalized URL */
      url?: string;
      /** @description The final URL after redirects. If the host differs from domain, the score reflects a redirected site. */
      finalUrl?: string;
      /** @description Overall score (0-100) */
      score?: number;
      /** @description Maximum possible score */
      maxScore?: number;
      /**
       * @description Letter grade (A+ >= 95, A >= 86, B >= 70, C >= 48, D >= 28, F < 28)
       * @enum {string}
       */
      grade?: "A+" | "A" | "B" | "C" | "D" | "F";
      /**
       * @description Completeness of the score. 'partial' = analysis still in progress (deep checks, relevance assessment, or summary generation); 'complete' = all post-processing done, score is final; 'stuck' = scan got stuck in partial for >30 minutes (worker likely failed) - the score will not advance on its own and the response will also include a `next_action` envelope pointing at POST /api/scan.
       * @enum {string}
       */
      analysisStatus?: "complete" | "partial" | "stuck";
      /** @description IDs of checks not yet resolved. Empty when analysisStatus is 'complete'. Poll GET /api/score/{domain} until this is empty for a final score. */
      pendingChecks?: string[];
      /** @description Call-to-action message based on score */
      ctaMessage?: string;
      /**
       * @description CTA tier
       * @enum {string}
       */
      ctaTier?: "top" | "high" | "mid" | "low";
      /** @description Breakdown by scoring layer */
      layers?: components["schemas"]["LayerResult"][];
      /**
       * Format: date-time
       * @description When the scan was performed
       */
      scannedAt?: string;
      /** @description Scan duration in milliseconds */
      durationMs?: number;
      /** @description Optional. A one-sentence natural-language verdict generated after analysis completes (e.g. "Stripe offers excellent developer resource discoverability and SDK availability, but lacks a published OpenAPI specification for agent integration."). Absent on older cached results or when the summary generation step did not run. */
      agenticSummary?: string;
      /**
       * @description Optional. How the scan is stored. 'domain' for a regular website, 'mcp' for an MCP server endpoint (handshake succeeded but no Apps support detected; also returned for catalog pages where we resolved a validated embedded MCP server URL), 'mcp-app' for an MCP server that negotiates the MCP Apps extension `io.modelcontextprotocol/ui` (or exposes `ui://` resources or tool `_meta.ui.resourceUri`), 'ephemeral' for a disposable scan (requested with `ephemeral: true`, or a public tunnel hostname) which is excluded from the leaderboard, coverage counts, research statistics, and score history and is deleted after a few days. The first three are detected; 'ephemeral' describes storage, and an ephemeral scan still runs the full check set for the kind it was detected as. Absent on older cached results.
       * @enum {string}
       */
      urlKind?: "domain" | "mcp" | "mcp-app" | "ephemeral";
      /** @description Optional. True when an MCP-family scan (urlKind 'mcp' or 'mcp-app') was short-circuited because the server returned 401/403 on the handshake. When set, 'layers' is empty and 'score' is 0; ora cannot evaluate agent-readiness for auth-gated MCP servers. UI surfaces an auth-required notice instead of the score hero. */
      mcpAuthRequired?: boolean;
      /**
       * @description Present (and always true) only when POST /api/scan answered from the freshness window with a stored result instead of running a scan. Absent on a live scan and on GET /api/score/{domain}, which is always a cached read.
       * @enum {boolean}
       */
      servedFromCache?: true;
      /** @description Age of the served stored result in seconds. Sent with servedFromCache, and mirrored in the Age response header. */
      resultAgeSeconds?: number;
      [key: string]: unknown;
    };
    LayerResult: {
      /** @description Layer identifier */
      id?: string;
      /** @description Layer display name */
      name?: string;
      /** @description Layer description */
      description?: string;
      checks?: components["schemas"]["CheckResult"][];
      /** @description Layer score */
      score?: number;
      /** @description Layer maximum possible score */
      maxScore?: number;
    };
    CheckResult: {
      /** @description Check identifier */
      id?: string;
      /** @description Check display name */
      name?: string;
      /** @description What this check tests */
      description?: string;
      /**
       * @description Check result status. 'pending' = deep scan not yet resolved; 'na' = not applicable for this product.
       * @enum {string}
       */
      status?: "pass" | "fail" | "warning" | "error" | "pending" | "na";
      /** @description Points earned */
      score?: number;
      /** @description Maximum points for this check */
      maxScore?: number;
      /** @description Human-readable explanation of the result */
      details?: string;
      /** @description Optional. Concrete fix that would make this check pass. Absent on passing checks and on some N/A rows. */
      recommendation?: string;
      /** @description Optional. Upside-only check: earning it raises the score, missing it never lowers it. An unearned bonus is excluded from the denominator entirely, so it reports score 0 without costing points. */
      bonus?: boolean;
      /**
       * @description Optional. 'verified' = evidence that agents rely on this signal, counts toward the 0-100 score. 'emerging' = early or low-adoption signal, shown but excluded from the denominator. Absent on older cached results.
       * @enum {string}
       */
      maturity?: "verified" | "emerging";
      /**
       * @description Optional. How strongly ora expects the check: 'required' = the baseline every product is measured against, 'recommended' = scored but outside the baseline, 'emerging' = excluded from the score. Display metadata derived from maturity plus the baseline - it never changes the score. Rank fixes by estScoreGain, not by tier.
       * @enum {string}
       */
      tier?: "required" | "recommended" | "emerging";
      /** @description Optional. Why the check does not apply to this product. Present on 'na' rows; the check is skipped, not deducted. */
      naReason?: string;
      /** @description Optional. Estimated points fully fixing this check would add to the overall 0-100 score, already normalized to the layer weight. Present on actionable (fail/warning) checks only; an estimate, not exact. This is the uplift signal - do not read maxScore minus score as score uplift. */
      estScoreGain?: number;
    };
    /** @description The competitive slice around a domain, drawn from the leaderboard. Both arrays hold lean rows (never full scan reports). The two slices are views over one ranked board and can overlap - for a top-5 domain the same row appears in both, so do not concatenate them naively. Mirrors the Competitive Analytics panel on the score page. */
    CompetitorSet: {
      /** @description The market category the competitors are drawn from. Always a real market category: for unclassified domains (Community, or no leaderboard row) the endpoint returns `competitors: null` instead of a set. */
      category?: string;
      /** @description Top 5 of the category by score, descending. */
      leaders?: components["schemas"]["Competitor"][];
      /** @description Up to 5 rows centred on the queried domain: 2 above, self, 2 below, within the same category pool. Always contains the `isSelf` row. */
      neighbors?: components["schemas"]["Competitor"][];
    };
    Competitor: {
      /** @description 1-based position on the category board the slices are drawn from. */
      rank?: number;
      /** @description Competitor domain */
      domain?: string;
      /** @description Competitor display name */
      name?: string;
      /** @description Agent-readiness score (0-100) */
      score?: number;
      /**
       * @description Letter grade
       * @enum {string}
       */
      grade?: "A+" | "A" | "B" | "C" | "D" | "F";
      /** @description True for the row representing the queried domain. Subdomains share their company's leaderboard row, so querying a subdomain marks the company's row as self. */
      isSelf?: boolean;
    };
    /**
     * @description Machine-parseable next step for an agent caller. Tells clients exactly which endpoint to hit and with what body to recover a missing or stuck score.
     * @example {
     *   "method": "POST",
     *   "endpoint": "/api/scan",
     *   "body": {
     *     "url": "stripe.com"
     *   },
     *   "description": "Trigger a fresh scan for this domain"
     * }
     */
    NextAction: {
      /**
       * @description HTTP method
       * @enum {string}
       */
      method: "POST";
      /** @description API path to call (e.g. /api/scan) */
      endpoint: string;
      /** @description Body to POST. For /api/scan this is { url }. */
      body: {
        /** @description Domain or URL to scan */
        url?: string;
      };
      /** @description Human-readable explanation of the recovery step */
      description: string;
    };
    /**
     * @description Returned by GET /api/score/{domain} (and similar read endpoints) when no cached score exists for the domain. The recovery envelope tells agent callers exactly how to recover: `next_action` on the default body, `nextAction` under `?format=audit`. Exactly one of the two is always present.
     * @example {
     *   "error": "No cached score for this domain",
     *   "code": "DOMAIN_NOT_SCANNED",
     *   "domain": "stripe.com",
     *   "next_action": {
     *     "method": "POST",
     *     "endpoint": "/api/scan",
     *     "body": {
     *       "url": "stripe.com"
     *     },
     *     "description": "Trigger a fresh scan for this domain"
     *   }
     * }
     */
    NotScannedResponse: {
      /** @description Human-readable error message */
      error: string;
      /**
       * @description Machine-readable error code
       * @enum {string}
       */
      code: "DOMAIN_NOT_SCANNED";
      /** @description Normalized domain that was looked up */
      domain: string;
      next_action?: components["schemas"]["NextAction"];
      /** @description Present instead of `next_action` when the request passed `?format=audit`. Same recovery step, camelCase and versioned, matching the `nextAction` on AuditScanResult / AuditScoreResult. */
      nextAction?: {
        /** @enum {string} */
        kind: "scan";
        /** @enum {string} */
        method: "POST";
        /** @enum {string} */
        endpoint: "/api/scan";
        body: {
          url: string;
        };
        /** @description Why this request is the recommended next step */
        reason: string;
      };
    };
    /**
     * @description Error envelope for the Agentic Resource Discovery (ARD) routes, matching the ARD registry spec (Appendix B). Distinct from ora's house ErrorResponse: ARD uses `errorCode` + `message` with the spec's standard codes.
     * @example {
     *   "errorCode": "RATE_LIMIT_EXCEEDED",
     *   "message": "Too many requests - please try again later"
     * }
     */
    ArdErrorResponse: {
      /**
       * @description Machine-readable ARD error code (spec Appendix B).
       * @enum {string}
       */
      errorCode: "INVALID_ARGUMENT" | "UNAUTHENTICATED" | "NOT_FOUND" | "RATE_LIMIT_EXCEEDED" | "INTERNAL_ERROR";
      /** @description Human-readable error explanation. */
      message: string;
      /** @description Optional structured validation detail (Zod flatten) on INVALID_ARGUMENT. */
      details?: {
        [key: string]: unknown;
      };
      /** @description Optional recovery hint (e.g. /api/scan on NOT_FOUND). */
      next?: string;
    };
    /**
     * @description ora's house error envelope. `error` is always present; the other fields depend on which guard rejected the request, so a client should branch on `code` / `retry_after_ms` being present rather than assume them.
     * @example {
     *   "error": "Daily scan limit reached (30 per day). Try again in about 4 hours.",
     *   "retry_after_ms": 14400000
     * }
     */
    ErrorResponse: {
      /** @description Error type or human-readable message (e.g. 'Not found', 'Rate limited') */
      error: string;
      /** @description Optional longer explanation with recovery steps */
      message?: string;
      /** @description Optional machine-readable error code (e.g. EPHEMERAL_CLOBBER, ENDPOINT_NOT_FOUND, RATE_LIMITED, INVALID_DOMAIN) */
      code?: string;
      /** @description Present on a 429 from the durable daily scan budget: milliseconds until a slot frees, the same interval the Retry-After header carries in seconds. Matches the deny body /api/journey/runs sends, so one client handler covers every rate-limited ora endpoint. */
      retry_after_ms?: number;
      /** @description Optional structured validation detail (Zod flatten) on a schema rejection. */
      details?: {
        [key: string]: unknown;
      };
    };
    DiscoverResult: {
      /** @description Product domain */
      domain?: string;
      /** @description Product name */
      name?: string;
      /** @description Product category */
      category?: string;
      /** @description Agent-readiness score (0-100) */
      score?: number;
      /** @enum {string} */
      grade?: "A" | "B" | "C" | "D" | "F";
      /** @description Product tags */
      tags?: string[];
      /** @description Relevance to your query */
      matchScore?: number;
    };
    AgentFeedback: {
      id?: number;
      domain?: string;
      /** @description Unique agent identifier */
      agent_id?: string;
      /** @description Original user request that led to this interaction */
      user_intent?: string | null;
      /** @description What the agent was trying to do */
      task_description?: string;
      /** @enum {string} */
      outcome?: "success" | "partial_failure" | "failure";
      /** @description Detailed feedback */
      content?: string;
      friction_points?: string[];
      /** @enum {string} */
      recommendation?: "recommend" | "neutral" | "not_recommend";
      /** @description Per-stage scores (1-5). Current funnel stages: discovery, identity, access, payments, experience. Legacy keys (integration, in-agent-experience) are still accepted for backward compatibility. */
      layer_scores?: {
        discovery?: number;
        identity?: number;
        access?: number;
        payments?: number;
        experience?: number;
        integration?: number;
        "in-agent-experience"?: number;
      } | null;
      /** Format: date-time */
      created_at?: string;
    };
    FeedbackStats: {
      /** @description Total feedback count */
      total?: number;
      /** @description Proportion of successful outcomes (0-1) */
      success_rate?: number;
      /** @description Proportion recommending (0-1) */
      recommend_rate?: number;
      outcomes?: {
        success?: number;
        partial_failure?: number;
        failure?: number;
      };
      recommendations?: {
        recommend?: number;
        neutral?: number;
        not_recommend?: number;
      };
    };
  };
  responses: never;
  parameters: never;
  requestBodies: never;
  headers: never;
  pathItems: never;
}

export type $defs = Record<string, never>;

export type external = Record<string, never>;

export interface operations {

  /**
   * Scan a domain, MCP server URL, or MCP App URL for agent-readiness
   * @description Runs a full agent-readiness scan on the given URL. Accepts a domain, MCP server URL, or MCP App URL (server that supports the MCP Apps extension `io.modelcontextprotocol/ui`) - the server auto-detects which kind of input was provided and selects the appropriate check set. Catalog-style listing pages are folded into the `mcp` kind by classifying the first validated embedded MCP URL. Returns score, grade, and detailed layer breakdown. The response includes an optional `urlKind` field indicating the detected kind ('domain', 'mcp', or 'mcp-app'). Scoring completes within the request, but deeper analysis can continue asynchronously afterwards: when the returned analysisStatus is 'partial', the response is a 202 Accepted with a Location header pointing at the polling endpoint for the remaining work; a 200 means analysis is already complete. For real-time progress updates, use GET /api/scan/stream which serves a text/event-stream. A complete stored result younger than the freshness window is returned as-is (servedFromCache, resultAgeSeconds, Age header) without running or persisting a scan - pass force: true, or widen/narrow maxAgeSeconds, to control that. Rate limited two ways: 10 requests per minute per IP (burst) and a durable daily scan budget shared with the other scan entry points - both return 429 with a Retry-After header.
   */
  scanDomain: {
    parameters: {
      query?: {
        /** @description Pass 1 to include a `competitors` object in the response: the category top-5 leaders plus the neighbor window (2 above / self / 2 below) drawn from the leaderboard. Returned only for domains with a market category - unclassified domains (Community, or no leaderboard row) get `competitors: null`. Omitted by default so the plain response stays lean. */
        competitors?: "1";
        /** @description Pass `audit` to receive the versioned, allowlisted audit shape (AuditScanResult / AuditScoreResult) instead of the default body: every field is documented, carries a `contractVersion`, and internal fields are dropped. Omit it and the response is unchanged from previous releases. On GET /api/scan/stream, only the terminal `scan_complete` event's `result` is projected - all other events are identical to the default stream. */
        format?: "audit";
      };
    };
    requestBody: {
      content: {
        "application/json": {
          /**
           * @description The domain, MCP server URL, or MCP app URL to scan. The server detects which kind of input was provided and runs the appropriate check set.
           * @example stripe.com
           */
          url: string;
          /**
           * @description Optional MCP server URL to test
           * @example
           */
          mcpUrl?: string;
          /**
           * @description How stale a stored result may be and still be returned instead of running a new scan. Defaults to 21600 (6 hours) and is clamped server-side to [3600, 86400] rather than rejected. See the 200 response's `servedFromCache` / `resultAgeSeconds` fields.
           * @example 21600
           */
          maxAgeSeconds?: number;
          /** @description Always run a live scan, whatever the age of the stored result. This is the only way to bypass the freshness window entirely. */
          force?: boolean;
          /** @description Store the result as disposable: it is excluded from the leaderboard, the sites-scanned coverage count, research statistics, and score history, is served with Cache-Control: no-store, and is deleted after a few days. Intended for a local site exposed through a tunnel, or any host that will not exist tomorrow. Public tunnel hostnames (trycloudflare.com, ngrok, and similar) are stored this way whether or not the flag is set. Rejected with 400 EPHEMERAL_CLOBBER when the domain already has a real stored scan, since a disposable result would replace it. */
          ephemeral?: boolean;
        };
      };
    };
    responses: {
      /** @description Scan completed successfully and analysis is complete. With `?competitors=1`, also carries a `competitors` object (category leaders + neighbor window drawn from the leaderboard). With `?format=audit` the body is `#/components/schemas/AuditScanResult` instead of the shape below. A response served from the freshness window instead of a new scan additionally carries `servedFromCache: true` and `resultAgeSeconds` (on both body shapes) plus an `Age` header, and consumed no scan. */
      200: {
        headers: {
          /** @description Present only on a freshness-window hit: the age of the returned stored result in seconds. */
          Age?: number;
        };
        content: {
          "application/json": components["schemas"]["ScanResult"] & ({
            /** @description Present only when the request passed ?competitors=1. Category leaders + neighbor window from the leaderboard, independent of analysis completeness. Null when the domain has no market category (unclassified / Community domains, or no leaderboard row) or when competitive data is temporarily unavailable. */
            competitors?: components["schemas"]["CompetitorSet"] | null;
          });
        };
      };
      /** @description Scan accepted and scored, but analysis is still in progress (analysisStatus is 'partial'). The body is the same shape as a 200 (including `competitors` when `?competitors=1` was passed, and `#/components/schemas/AuditScanResult` when `?format=audit` was passed). Poll the Location header URL (GET /api/score/{domain}) until analysisStatus is 'complete' and pendingChecks is empty. */
      202: {
        headers: {
          /** @description URL of the scan result resource to poll, e.g. /api/score/stripe.com */
          Location?: string;
        };
        content: {
          "application/json": components["schemas"]["ScanResult"] & ({
            /** @description Present only when the request passed ?competitors=1. Category leaders + neighbor window from the leaderboard, independent of analysis completeness. Null when the domain has no market category (unclassified / Community domains, or no leaderboard row) or when competitive data is temporarily unavailable. */
            competitors?: components["schemas"]["CompetitorSet"] | null;
          });
        };
      };
      /** @description Invalid input, or `ephemeral: true` for a domain that already has a real stored scan (body carries `code: "EPHEMERAL_CLOBBER"`; storing a disposable result would replace the real one). */
      400: {
        content: {
          "application/json": components["schemas"]["ErrorResponse"];
        };
      };
      /** @description Rate limit exceeded - either the 10-per-minute burst cap or the durable daily quota (30 scans per rolling 24h per IP; 6 per day for force=true). Cache-served responses never count against the daily quota. The response includes a Retry-After header indicating seconds until the next request is allowed, and a JSON body with error and retry_after_ms. */
      429: {
        headers: {
          /** @description Seconds until next allowed request */
          "Retry-After"?: number;
        };
        content: {
          "application/json": components["schemas"]["ErrorResponse"];
        };
      };
      /** @description Scan failed */
      500: {
        content: never;
      };
    };
  };
  /**
   * Instant-trigger entry point for the v2 async scan lifecycle (rollout, gated)
   * @description Phase 1b of the scan lifecycle redesign (see docs/plans/scan-lifecycle-prd.md). Validates the input (rate-limit 10/min/IP, Zod, isValidUrl, reachability, URL-kind classification), de-dupes against any in-progress v2 row for the domain, INSERTs the scans row at status="pending" with is_current=true and flow_version='v2', and returns the scanId plus a pollUrl. The downstream pipeline (Stage 1 context + static checks, Stage 2 deep checks via the Fly worker, Stage 3 finalize) is NOT YET WIRED in this PR - rows created here stay at status="pending" until follow-up PRs ship Stage 1 and the recovery cron. The endpoint is gated behind the SCAN_V2_ENABLED env flag and returns 503 in environments where it is unset, so external callers must not rely on it before cutover. The v1 read paths (/api/score/[domain], the leaderboard, the sitemap) already filter out flow_version='v2' rows.
   */
  scanDomainV2: {
    requestBody: {
      content: {
        "application/json": {
          /**
           * @description The domain, MCP server URL, or MCP App URL to scan.
           * @example stripe.com
           */
          url: string;
          /**
           * @description Optional explicit MCP server URL. When set, drives URL-kind classification.
           * @example
           */
          mcpUrl?: string;
          /** @description Same flag as POST /api/scan: store the row as disposable, excluded from the leaderboard, coverage counts, research statistics, and score history. Public tunnel hostnames are stored this way whether or not the flag is set. Unlike POST /api/scan, this endpoint does NOT refuse the flag with 400 EPHEMERAL_CLOBBER - the clobber guard rides on the freshness read, which v2 does not perform yet. */
          ephemeral?: boolean;
        };
      };
    };
    responses: {
      /** @description Duplicate hit - an in-progress v2 row already exists for the domain. Returns its scanId and current status so the client can poll. */
      200: {
        content: {
          "application/json": {
            /** @example 7 */
            scanId: number;
            /** @example running */
            status: string;
            /** @example /api/v2/scan/7 */
            pollUrl: string;
          };
        };
      };
      /** @description v2 scan row created. Client should poll pollUrl for progress. */
      201: {
        content: {
          "application/json": {
            /** @example 42 */
            scanId: number;
            /** @example pending */
            status: string;
            /** @example /api/v2/scan/42 */
            pollUrl: string;
          };
        };
      };
      /** @description Invalid input (schema or domain). */
      400: {
        content: {
          "application/json": components["schemas"]["ErrorResponse"];
        };
      };
      /** @description Domain unreachable or URL-kind classification failed. */
      422: {
        content: {
          "application/json": components["schemas"]["ErrorResponse"];
        };
      };
      /** @description Rate limit exceeded - either the 10-per-minute burst cap or the durable daily quota (30 scans per rolling 24h per IP), which this endpoint spends from the same budget as the v1 scan routes so a caller cannot drain it twice by switching endpoints. The response carries a Retry-After header; a durable deny also carries retry_after_ms in the body. */
      429: {
        headers: {
          /** @description Seconds until next allowed request */
          "Retry-After"?: number;
        };
        content: {
          "application/json": components["schemas"]["ErrorResponse"];
        };
      };
      /** @description Internal error. */
      500: {
        content: never;
      };
      /** @description Endpoint disabled in this environment (SCAN_V2_ENABLED is not set). */
      503: {
        content: {
          "application/json": components["schemas"]["ErrorResponse"];
        };
      };
    };
  };
  /**
   * Stream an agent-readiness scan as Server-Sent Events
   * @description Runs a full agent-readiness scan on the given URL and streams progress as text/event-stream. Accepts a domain, MCP server URL, or MCP App URL (server that supports the MCP Apps extension `io.modelcontextprotocol/ui`) - the server auto-detects which kind of input was provided and selects the appropriate check set. Catalog-style listing pages are folded into the `mcp` kind by classifying the first validated embedded MCP URL. The stream emits a `kind_detecting` event immediately after the cheap reachability probe, followed by exactly one `kind_detected` event with payload `{ kind: 'domain' | 'mcp' | 'mcp-app', mcpUrl?: string, embeddedMcpUrls?: string[], hint?: string }` once URL-kind detection resolves. Subsequent events include `scan_init`, `layer_start`, `check_start`, `check_complete`, `layer_complete`, and finally `scan_complete` whose payload mirrors the ScanResult schema (including the optional `urlKind` field indicating the detected kind). The same freshness gate as POST /api/scan applies: a hit is a `kind_detected` frame followed by the terminal `scan_complete` event, rather than a full run. Rate limited two ways: 10 requests per minute per IP (burst) and a durable daily scan budget shared with the other scan entry points - both return 429 with a Retry-After header.
   */
  scanDomainStream: {
    parameters: {
      query: {
        /** @description The domain, MCP server URL, or MCP app URL to scan. The server detects which kind of input was provided and runs the appropriate check set. */
        domain: string;
        /** @description Optional explicit MCP server URL to test alongside the scan */
        mcp?: string;
        /** @description Same freshness window as POST /api/scan: how stale a stored result may be and still be streamed back instead of running a new scan. Defaults to 21600 (6 hours), clamped to [3600, 86400]. */
        maxAgeSeconds?: number;
        /** @description Pass 1 to always run a live scan, whatever the age of the stored result. */
        force?: "1";
        /** @description Pass 1 to store the result as disposable - the same flag POST /api/scan takes in its body, with the same 400 EPHEMERAL_CLOBBER refusal when the domain already has a real stored scan. */
        ephemeral?: "1";
        /** @description Pass `audit` to receive the versioned, allowlisted audit shape (AuditScanResult / AuditScoreResult) instead of the default body: every field is documented, carries a `contractVersion`, and internal fields are dropped. Omit it and the response is unchanged from previous releases. On GET /api/scan/stream, only the terminal `scan_complete` event's `result` is projected - all other events are identical to the default stream. */
        format?: "audit";
      };
    };
    responses: {
      /** @description Server-Sent Events stream of scan progress. With `?format=audit`, the terminal `scan_complete` event's `result` is `#/components/schemas/AuditScanResult`; every other event is unchanged. When a stored result inside the freshness window answers the request, the stream is a `kind_detected` frame followed by a terminal `scan_complete` event carrying `servedFromCache: true` and `resultAgeSeconds`, and the response carries an `Age` header. */
      200: {
        content: {
          "text/event-stream": string;
        };
      };
      /** @description Missing or invalid domain parameter */
      400: {
        content: never;
      };
      /** @description Rate limit exceeded - either the 10-per-minute burst cap or the durable daily quota (30 scans per rolling 24h per IP; 6 per day for force=1). The response carries a Retry-After header with the seconds until the caller's window frees up. */
      429: {
        headers: {
          /** @description Seconds until next allowed request */
          "Retry-After"?: number;
        };
        content: never;
      };
    };
  };
  /**
   * Get cached score for a domain
   * @description Returns the most recent cached scan result for the given domain. Read-only: never triggers a scan. On miss (404) or when the previous scan got stuck mid-flight (200 with `analysisStatus: "stuck"`), the response carries a structured `next_action` envelope pointing at `POST /api/scan` so agent callers have a machine-parseable next step. Successful responses are cached for 1 hour; stuck, 404, and ephemeral (disposable, `urlKind: "ephemeral"`) responses are uncached (`Cache-Control: no-store`) so a successful re-scan is observable immediately and a deleted disposable row is never served from cache. Rate limited to 10 requests per minute per IP - returns 429 if exceeded.
   */
  getScore: {
    parameters: {
      query?: {
        /** @description Pass 1 to include a `competitors` object in the response: the category top-5 leaders plus the neighbor window (2 above / self / 2 below) drawn from the leaderboard. Returned only for domains with a market category - unclassified domains (Community, or no leaderboard row) get `competitors: null`. Omitted by default so the plain response stays lean. */
        competitors?: "1";
        /** @description Pass `audit` to receive the versioned, allowlisted audit shape (AuditScanResult / AuditScoreResult) instead of the default body: every field is documented, carries a `contractVersion`, and internal fields are dropped. Omit it and the response is unchanged from previous releases. On GET /api/scan/stream, only the terminal `scan_complete` event's `result` is projected - all other events are identical to the default stream. */
        format?: "audit";
      };
      path: {
        /** @description The domain to look up (e.g. stripe.com). URL-encoded full URLs are normalized to their hostname. */
        domain: string;
      };
    };
    responses: {
      /** @description Cached scan result. With `?competitors=1`, also carries a `competitors` object (category leaders + neighbor window drawn from the leaderboard). When `analysisStatus` is `"stuck"`, the body also includes a `next_action` envelope. With `?format=audit` the body is `#/components/schemas/AuditScoreResult` and the recovery envelope is the camelCase `nextAction`. */
      200: {
        content: {
          "application/json": components["schemas"]["ScanResult"] & ({
            /** @description Present only when the request passed ?competitors=1. Category leaders + neighbor window from the leaderboard, independent of analysis completeness. Null when the domain has no market category (unclassified / Community domains, or no leaderboard row) or when competitive data is temporarily unavailable. */
            competitors?: components["schemas"]["CompetitorSet"] | null;
          }) & {
            /** @description Present only when analysisStatus is 'stuck' (a scan that stayed partial for over 30 minutes). Machine-parseable next step to recover the score. Under `?format=audit` this is the camelCase `nextAction` on AuditScoreResult instead. */
            next_action?: components["schemas"]["NextAction"];
          };
        };
      };
      /** @description No cached score for this domain. Body includes `code: "DOMAIN_NOT_SCANNED"` and a `next_action` pointing at `POST /api/scan` (`nextAction`, camelCase, under `?format=audit`). */
      404: {
        content: {
          "application/json": components["schemas"]["NotScannedResponse"];
        };
      };
      /** @description Rate limit exceeded - max 10 requests per minute per IP. The response carries a Retry-After header with the seconds until the oldest request in the window ages out. */
      429: {
        headers: {
          /** @description Seconds until next allowed request */
          "Retry-After"?: number;
        };
        content: {
          "application/json": components["schemas"]["ErrorResponse"];
        };
      };
      /** @description Database unavailable */
      500: {
        content: never;
      };
    };
  };
  /**
   * Get SVG badge for a domain
   * @description Returns an SVG badge showing the domain's ora score and grade. Embed in READMEs or websites. Cached for 1 hour.
   */
  getBadge: {
    parameters: {
      path: {
        /** @description The domain to get a badge for */
        domain: string;
      };
    };
    responses: {
      /** @description SVG badge image */
      200: {
        content: {
          "image/svg+xml": string;
        };
      };
      /** @description No score found for this domain */
      404: {
        content: never;
      };
    };
  };
  /**
   * Discover agent-ready products by intent
   * @description Find the most agent-ready products for a given need. Describe what you're looking for and get products ranked by agent-readiness score. Cached for 5 minutes.
   */
  discoverProducts: {
    parameters: {
      query: {
        /** @description What you need - describe the task or product category (e.g. 'send transactional emails', 'CRM with API') */
        intent: string;
        /** @description Max results to return */
        limit?: number;
      };
    };
    responses: {
      /** @description Matching products ranked by relevance and agent-readiness */
      200: {
        content: {
          "application/json": {
            intent?: string;
            results?: components["schemas"]["DiscoverResult"][];
            total?: number;
          };
        };
      };
      /** @description Missing intent parameter */
      400: {
        content: never;
      };
    };
  };
  /**
   * Report an issue with a specific check result
   * @description Submit feedback about an inaccurate check result. Accepts both human and agent submissions. Agent submissions require HATCHA verification. Check state (score, status, details) is snapshotted server-side from the latest scan.
   */
  reportCheckIssue: {
    requestBody: {
      content: {
        "application/json": {
          /**
           * @description Submission source. Agent submissions require HATCHA verification fields.
           * @enum {string}
           */
          reporterType: "human" | "agent";
          /** @description The product domain (e.g. stripe.com) */
          domain: string;
          /** @description The check ID to report (e.g. openapi-spec) */
          checkId: string;
          /**
           * @description Why the check result seems wrong
           * @enum {string}
           */
          reason: "false_pass" | "false_fail" | "wrong_details" | "outdated" | "other";
          /** @description Description of the issue */
          message: string;
          /** @description Human only, optional. We'll notify you if we find and fix the issue. */
          reporterEmail?: string;
          /** @description Agent only, required. Agent identifier (e.g. claude-code-a8f3b1e92d) */
          agentId?: string;
          /** @description Agent only, required. Token from get_verification_challenge. */
          verificationToken?: string;
          /** @description Agent only, required. Solved HATCHA challenge answer. */
          verificationAnswer?: string;
        };
      };
    };
    responses: {
      /** @description Feedback submitted successfully */
      200: {
        content: {
          "application/json": {
            ok?: boolean;
            /** @description The feedback record ID */
            id?: number;
          };
        };
      };
      /** @description Invalid payload or unknown checkId */
      400: {
        content: never;
      };
      /** @description Agent verification failed */
      401: {
        content: never;
      };
      /** @description No scan found for domain, or check not in latest scan */
      404: {
        content: never;
      };
      /** @description Rate limit exceeded */
      429: {
        content: never;
      };
      /** @description Agent verification unavailable */
      503: {
        content: never;
      };
    };
  };
  /**
   * Send a message to the ora team
   * @description Submit a contact-form message. Open endpoint - no authentication required. Sends an inquiry email to the ora team and an auto-responder to the submitter. Rate limited to 3 submissions per IP per 10 minutes. Agents are welcome to use this endpoint, though email is the simpler path for most cases.
   */
  submitContactInquiry: {
    requestBody: {
      content: {
        "application/json": {
          /** @description Sender's name */
          name: string;
          /**
           * Format: email
           * @description Sender's email - used as Reply-To on the inquiry and as the destination for the auto-responder
           */
          email: string;
          /** @description The message body */
          message: string;
        };
      };
    };
    responses: {
      /** @description Submission accepted */
      200: {
        content: {
          "application/json": {
            ok?: boolean;
          };
        };
      };
      /** @description Invalid input */
      400: {
        content: {
          "application/json": components["schemas"]["ErrorResponse"];
        };
      };
      /** @description Too many submissions from this IP */
      429: {
        content: never;
      };
      /** @description Failed to send the inquiry email */
      500: {
        content: never;
      };
    };
  };
  /**
   * Search agentic resources (Agentic Resource Discovery)
   * @description Runs an Agentic Resource Discovery (ARD) search over ora's catalog of agent-ready resources. Returns resources ranked by relevance to a free-text query, with optional field filters and federation control. The per-result `score` is a readiness-weighted relevance score (0-100): match quality for the query (dominant), multiplied by a 0.6-1.0 factor from the domain's agent-readiness. It is distinct from the raw agent-readiness score returned by POST /api/scan and GET /api/score/{domain}. Rate limited to 30 requests per minute per IP - returns 429 if exceeded.
   */
  ardSearch: {
    requestBody: {
      content: {
        "application/json": {
          query: {
            /** @description Free-text search query. May be omitted when filter is present (a filter-only browse); at least one of text/filter is required. */
            text?: string;
            /** @description Optional field filters: a map of field name to an allowed value (a scalar string or an array of strings; OR within a key, AND across keys). */
            filter?: {
              [key: string]: string | string[];
            };
          };
          /**
           * @description Federation policy. 'none': ora's own index only. 'referrals': ora's results plus referrals[] pointers to upstream registries. 'auto': ora merges upstream registry results into results (each tagged with its upstream source); merging is off by default (server-gated) and degrades to own-results-only when disabled.
           * @default auto
           * @enum {string}
           */
          federation?: "auto" | "referrals" | "none";
          /**
           * @description Results per page (1-100, default 10).
           * @default 10
           */
          pageSize?: number;
          /** @description Opaque pagination token from a previous response. */
          pageToken?: string;
        };
      };
    };
    responses: {
      /** @description The ARD SearchResponse: { results, referrals, pageToken? }. pageToken is omitted when the result set is exhausted (never null). ora is non-federating, so referrals is []. Each result's trustManifest.attestations[0] is a reference { type, uri, mediaType } to GET /api/ard/attestation/{domain}, not inlined claims. Results from ora's own index also carry an `oraScorecard` vendor extension ({ score?, grade?, category?, checkedAt? }): agent-readiness score/grade only when ora has scored the domain (never a fabricated 0/F), category whenever ora has classified it - all distinct from the relevance `score`; the signed claim remains the attestation endpoint. */
      200: {
        content: never;
      };
      /** @description INVALID_ARGUMENT - malformed query body. */
      400: {
        content: {
          "application/json": components["schemas"]["ArdErrorResponse"];
        };
      };
      /** @description RATE_LIMIT_EXCEEDED - max 30 requests per minute per IP. */
      429: {
        content: {
          "application/json": components["schemas"]["ArdErrorResponse"];
        };
      };
      /** @description INTERNAL_ERROR */
      500: {
        content: {
          "application/json": components["schemas"]["ArdErrorResponse"];
        };
      };
    };
  };
  /**
   * Faceted exploration of agentic resources (Agentic Resource Discovery)
   * @description Returns facet aggregations (counts per field value) for an Agentic Resource Discovery (ARD) result set. Use this to build filter UIs over the ARD catalog. An optional query narrows the set before faceting. Rate limited to 30 requests per minute per IP - returns 429 if exceeded.
   */
  ardExplore: {
    requestBody: {
      content: {
        "application/json": {
          /** @description Optional query to narrow the set before faceting (same shape as POST /api/ard/search's query). */
          query?: {
            /** @description Free-text search query. */
            text?: string;
            filter?: {
              [key: string]: string[];
            };
          };
          resultType: {
            facets: {
                /** @description The resource field to aggregate on. */
                field: string;
                /** @description Max facet values to return for this field. */
                limit?: number;
                /** @description Drop buckets whose count is below this (default 1). */
                minCount?: number;
              }[];
          };
        };
      };
    };
    responses: {
      /** @description Facet aggregations for the matched resource set. */
      200: {
        content: never;
      };
      /** @description INVALID_ARGUMENT - malformed query body. */
      400: {
        content: {
          "application/json": components["schemas"]["ArdErrorResponse"];
        };
      };
      /** @description RATE_LIMIT_EXCEEDED - max 30 requests per minute per IP. */
      429: {
        content: {
          "application/json": components["schemas"]["ArdErrorResponse"];
        };
      };
      /** @description INTERNAL_ERROR */
      500: {
        content: {
          "application/json": components["schemas"]["ArdErrorResponse"];
        };
      };
    };
  };
  /**
   * List discoverable agentic resources (Agentic Resource Discovery)
   * @description Returns a paginated listing of the agentic resources ora publishes for Agentic Resource Discovery (ARD). Deterministic browsing via the spec EBNF `filter` expression and `orderBy`. Rate limited to 60 requests per minute per IP - returns 429 if exceeded.
   */
  ardListAgents: {
    parameters: {
      query?: {
        /** @description Results per page (1-100, default 20). */
        pageSize?: number;
        /** @description Opaque pagination token from a previous response. */
        pageToken?: string;
        /** @description EBNF filter expression, e.g. "type = 'application/mcp-server-card+json' AND updatedAfter > '2026-01-01'". Fields: type, displayName, publisherId, tags, updatedAt. Operators: = != > < >= <=, conditions joined by AND. An unsupported field/operator/syntax returns 400 INVALID_ARGUMENT. */
        filter?: string;
        /** @description Sort expression "<field> [ASC|DESC]", e.g. "displayName DESC". Fields: displayName, updatedAt, identifier, type. */
        orderBy?: string;
      };
    };
    responses: {
      /** @description The ARD ListResponse: { items, total, pageToken? } (pageToken omitted when exhausted). */
      200: {
        content: never;
      };
      /** @description INVALID_ARGUMENT - unsupported or malformed filter/orderBy. */
      400: {
        content: {
          "application/json": components["schemas"]["ArdErrorResponse"];
        };
      };
      /** @description RATE_LIMIT_EXCEEDED - max 60 requests per minute per IP. */
      429: {
        content: {
          "application/json": components["schemas"]["ArdErrorResponse"];
        };
      };
      /** @description INTERNAL_ERROR */
      500: {
        content: {
          "application/json": components["schemas"]["ArdErrorResponse"];
        };
      };
    };
  };
  /**
   * Get a signed scorecard attestation for a domain (Agentic Resource Discovery)
   * @description Returns an Agentic Resource Discovery (ARD) scorecard attestation for the given domain. When ora has an attestation signing key configured, the payload is returned as an EdDSA detached JWS that verifies against the public JWK set at GET /api/ard/jwks (also served at /.well-known/jwks.json); without a configured key the attestation is returned unsigned. Rate limited to 60 requests per minute per IP - returns 429 if exceeded.
   */
  ardGetAttestation: {
    parameters: {
      path: {
        /** @description The domain to attest (e.g. stripe.com). */
        domain: string;
      };
    };
    responses: {
      /** @description The scorecard attestation - an EdDSA detached JWS when signing is configured, otherwise an unsigned payload. */
      200: {
        content: never;
      };
      /** @description INVALID_ARGUMENT - malformed domain. */
      400: {
        content: {
          "application/json": components["schemas"]["ArdErrorResponse"];
        };
      };
      /** @description NOT_FOUND - no cached score for this domain. Body also carries `next` pointing at POST /api/scan. */
      404: {
        content: {
          "application/json": components["schemas"]["ArdErrorResponse"];
        };
      };
      /** @description RATE_LIMIT_EXCEEDED - max 60 requests per minute per IP. */
      429: {
        content: {
          "application/json": components["schemas"]["ArdErrorResponse"];
        };
      };
      /** @description INTERNAL_ERROR */
      500: {
        content: {
          "application/json": components["schemas"]["ArdErrorResponse"];
        };
      };
    };
  };
  /**
   * Get the ARD registry service descriptor (Agentic Resource Discovery)
   * @description Returns the application/ai-registry+json service descriptor at ora's registry base URL - the document ora advertises in its AI Catalog's application/ai-registry+json entry. Names the live search/explore/agents/catalog endpoints, the pageToken pagination model (max page size 100), the filter/orderBy/facet fields, the served media types, and the federation modes. A crawler that ingests /.well-known/ai-catalog.json and follows the registry entry lands here.
   */
  ardGetRegistryDescriptor: {
    responses: {
      /** @description The ARD registry service descriptor. */
      200: {
        content: never;
      };
    };
  };
  /**
   * Get ora's AI Catalog manifest (Agentic Resource Discovery)
   * @description Returns ora's AI Catalog manifest for Agentic Resource Discovery (ARD). Also served at /.well-known/ai-catalog.json via a rewrite, and advertised by a Link header with rel="ai-catalog" on the homepage.
   */
  ardGetCatalog: {
    responses: {
      /** @description The ARD AI Catalog manifest. */
      200: {
        content: never;
      };
      /** @description Catalog generation failed */
      500: {
        content: never;
      };
    };
  };
  /**
   * Get the full ARD catalog dump (Agentic Resource Discovery)
   * @description Returns every indexed Agentic Resource Discovery (ARD) entry - ora's own products, their detected MCP server / skill resources, and crawled external catalogs - in one uncapped document { version, generatedAt, publisher, data }, for bulk ingest without paging through /api/ard/agents. Shares the same index pipeline as /agents and /explore. Sends Access-Control-Allow-Origin: * and is CDN-cached. Rate limited to 60 requests per minute per IP - returns 429 if exceeded. Also reachable at directory.ora.ai/api.json.
   */
  ardGetCatalogDump: {
    responses: {
      /** @description The full-catalog dump: { version, generatedAt, publisher, data }. */
      200: {
        content: never;
      };
      /** @description RATE_LIMIT_EXCEEDED - max 60 requests per minute per IP. */
      429: {
        content: {
          "application/json": components["schemas"]["ArdErrorResponse"];
        };
      };
    };
  };
  /**
   * Get the public JWK set for verifying signed attestations (Agentic Resource Discovery)
   * @description Returns the public JSON Web Key Set used to verify ora's signed Agentic Resource Discovery (ARD) scorecard attestations. Also served at /.well-known/jwks.json via a rewrite.
   */
  ardGetJwks: {
    responses: {
      /** @description The public JWK set ({ keys: [...] }). */
      200: {
        content: never;
      };
      /** @description JWKS generation failed */
      500: {
        content: never;
      };
    };
  };
  /**
   * Run an agent journey against a domain
   * @description Triggers a real agent run: an AI agent (harness + model) attempts a curated task (an intent) against the given domain, and ora records the trajectory and derives insights. Two-step flow: this endpoint returns fast with a run record whose `stream_url` serves the live trajectory as Server-Sent Events; poll GET /api/journey/runs/{id} instead if you do not want the stream. Only curated intents run publicly (see GET /api/journey/intents) - the server derives the actual agent prompt from the intent id, so no free-text prompt can reach the engine. Only publicly runnable agents are accepted (see GET /api/journey/agents). Unknown body fields are rejected (strict schema). Rate limited three ways: a 20-per-minute burst cap per IP; a per-target cap of 5 runs per rolling 24h per (domain, intent, harness, model) - when a target is over the cap the response is HTTP 200 with the most recent stored run for that target (`rate_limited: true`) plus Retry-After, the freshness analog: a denied trigger still returns the newest result and consumes nothing; and a durable per-caller cap of 10 runs per rolling 24h per IP - exceeded, that one is a real 429 with `retry_after_ms` (there is no cached result to serve for a caller). Successful and capped responses carry X-RateLimit-Limit / X-RateLimit-Remaining / X-RateLimit-Reset headers for the per-target window. Note: journey insights use the 5-layer journey taxonomy (discovery, identity, access, payments, experience), deliberately distinct from the 4 scoring layers of the audit report (POST /api/scan) - never map between the two.
   */
  createJourneyRun: {
    requestBody: {
      content: {
        "application/json": {
          intent: {
            /**
             * @description Curated intent to execute (GET /api/journey/intents lists them with labels)
             * @enum {string}
             */
            intent_id: "pricing" | "signup" | "api-docs" | "integrate" | "support" | "evaluate" | "discover-trust" | "agent-access" | "understand-offering" | "find-integration" | "inspect-integration" | "production-readiness" | "authenticate" | "transact" | "act-for-user" | "operate-site";
            /**
             * @description Target domain (e.g. stripe.com). Validated like a scan target: IP literals, localhost, and malformed hosts are rejected with code INVALID_DOMAIN.
             * @example stripe.com
             */
            domain?: string;
          };
          /**
           * @description Agent harness wire name. The (harness, model) pair must resolve to a publicly runnable agent from GET /api/journey/agents, else 400 UNSUPPORTED_AGENT.
           * @enum {string}
           */
          harness: "claude-agent-sdk" | "openai-agents" | "ash";
          /**
           * @description Model the harness drives (e.g. claude-haiku-4-5)
           * @example claude-haiku-4-5
           */
          model: string;
        };
      };
    };
    responses: {
      /** @description Per-target cap hit - the freshness analog, not an error: the body is the most recent stored run for this exact (domain, intent, harness, model) target, marked `rate_limited: true`, so CI and agents get the newest result without spending a run. When the served run has finished, the body also carries `verdict` and `step_count`. Replay it via its `stream_url`. */
      200: {
        headers: {
          /** @description Seconds until the target's oldest in-window run ages out */
          "Retry-After"?: number;
          /** @description Per-target window size (5 runs per rolling 24h) */
          "X-RateLimit-Limit"?: number;
          /** @description Always 0 on a capped response */
          "X-RateLimit-Remaining"?: number;
          /** @description Unix seconds when the next per-target slot frees */
          "X-RateLimit-Reset"?: number;
        };
        content: {
          "application/json": components["schemas"]["JourneyCappedRun"];
        };
      };
      /** @description Run created and dispatched. Open `stream_url` (SSE) to watch the trajectory live, or poll GET /api/journey/runs/{id}. */
      201: {
        headers: {
          /** @description Per-target window size (5 runs per rolling 24h) */
          "X-RateLimit-Limit"?: number;
          /** @description Runs left in this target's window, including this one's consumption */
          "X-RateLimit-Remaining"?: number;
          /** @description Unix seconds when the next per-target slot frees. Absent when the window was empty. */
          "X-RateLimit-Reset"?: number;
        };
        content: {
          "application/json": components["schemas"]["JourneyRun"];
        };
      };
      /** @description Invalid input: schema failure (including unknown body fields - the contract is strict), `code: "INVALID_DOMAIN"` for a domain the scanner would reject, or `code: "UNSUPPORTED_AGENT"` for a (harness, model) pair that is not publicly runnable. */
      400: {
        content: {
          "application/json": components["schemas"]["ErrorResponse"];
        };
      };
      /** @description Caller limit exceeded - the 20-per-minute burst cap, or the durable per-caller budget (10 runs per rolling 24h per IP; body carries `retry_after_ms`). Per-TARGET saturation is the 200 above, not this. */
      429: {
        headers: {
          /** @description Seconds until next allowed request */
          "Retry-After"?: number;
        };
        content: {
          "application/json": components["schemas"]["ErrorResponse"];
        };
      };
      /** @description The run engine could not be reached; nothing was created or consumed. */
      503: {
        content: {
          "application/json": components["schemas"]["ErrorResponse"];
        };
      };
    };
  };
  /**
   * Get a journey run record (non-streaming)
   * @description Returns the run record by id. While the run executes, `status` is 'running' - open the stream or poll. Once finished and persisted, `status` is 'succeeded' and the body carries `verdict`, `step_count` (billable steps - the same counter run pricing uses), and the full `result` (trajectory + insight + signals). `status` 'failed' is terminal with no result. `result` is present iff status is 'succeeded'.
   */
  getJourneyRun: {
    parameters: {
      path: {
        /** @description The run id from POST /api/journey/runs */
        id: string;
      };
    };
    responses: {
      /** @description The run record; plus `result` once succeeded. */
      200: {
        content: {
          "application/json": components["schemas"]["JourneyRunDetail"];
        };
      };
      /** @description Unknown run id */
      404: {
        content: never;
      };
    };
  };
  /**
   * Stream a journey run's trajectory as Server-Sent Events
   * @description The live trajectory SSE for a run created by POST /api/journey/runs. Event names (stable): `run_id` ({ run_id }), then progressive `trajectory` frames (cumulative snapshots, each a { steps, ... } object of JourneyTrajectoryStep items), optionally `processing` ({ message }) while insights generate, and finally exactly one of `result` (a JourneyRunResult) or `error` ({ message }). Reopening the stream of a finished run replays the stored result instead of re-executing the agent (pass ?replay=1 for paced frames, plus &quick=1 to skip the startup delay); a plain reopen renders the finished journey in one frame. Long-lived: a live run can hold the connection for many minutes (function ceiling 800s). Rate limited 20 per minute per IP.
   */
  streamJourneyRun: {
    parameters: {
      query?: {
        /** @description Pass 1 to replay a finished run as paced progressive frames (the animated replay). */
        replay?: "1";
        /** @description With replay=1: skip the startup delay. */
        quick?: "1";
      };
      path: {
        /** @description The run id */
        id: string;
      };
    };
    responses: {
      /** @description Server-Sent Events stream: run_id -> trajectory* -> processing? -> result | error. */
      200: {
        content: {
          "text/event-stream": string;
        };
      };
      /** @description Unknown run id */
      404: {
        content: never;
      };
      /** @description Rate limit exceeded - max 20 requests per minute per IP. */
      429: {
        content: {
          "application/json": components["schemas"]["ErrorResponse"];
        };
      };
    };
  };
  /**
   * List the curated journey intents
   * @description The curated tasks a public journey run can execute. Each entry carries the stable `id` (what POST /api/journey/runs takes), a short `label`, a one-line `hint`, and the user-facing `template` phrasing. Cached statically.
   */
  listJourneyIntents: {
    responses: {
      /** @description Curated intents plus the default id. */
      200: {
        content: {
          "application/json": {
            intents?: {
                /** @description Stable intent id */
                id?: string;
                /** @description Short picker label */
                label?: string;
                /** @description One-line description of the job to be done */
                hint?: string;
                /** @description User-facing phrasing of the task */
                template?: string;
              }[];
            defaultId?: string;
          };
        };
      };
    };
  };
  /**
   * List the publicly runnable journey agents
   * @description The agents an anonymous POST /api/journey/runs accepts - every listed (harness, model) pair is publicly runnable. Entries carry display metadata (label, variant, brand, blurb) plus the wire fields `harness` and `model`. Cached statically.
   */
  listJourneyAgents: {
    responses: {
      /** @description Runnable agents plus the default picker id. */
      200: {
        content: {
          "application/json": {
            agents?: {
                /** @description Picker id */
                id?: string;
                label?: string;
                variant?: string;
                brand?: string;
                /** @description Wire harness for POST /api/journey/runs */
                harness?: string;
                /** @description Wire model for POST /api/journey/runs */
                model?: string;
                agentLabel?: string;
                blurb?: string;
                badge?: string;
              }[];
            defaultId?: string;
          };
        };
      };
    };
  };
  /**
   * Get agent feedback for a product
   * @description Returns feedback submitted by AI agents about their experience using a product. Includes aggregate stats and individual reviews.
   */
  getAgentFeedback: {
    parameters: {
      path: {
        /** @description The product domain (e.g. stripe.com) */
        domain: string;
      };
    };
    responses: {
      /** @description Agent feedback with stats */
      200: {
        content: {
          "application/json": {
            domain?: string;
            stats?: components["schemas"]["FeedbackStats"];
            feedback?: components["schemas"]["AgentFeedback"][];
          };
        };
      };
    };
  };
}
